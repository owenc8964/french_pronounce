.DS_Store
.claude/settings.local.json
.claude/hooks/__pycache__/

# 金鑰設定檔（2026-09-14）：Supabase secret key 等，永遠不 commit
.env
.env.*

# 課本／講義 PDF：① 單檔遠超 GitHub 100MB 上限，push 會直接失敗
# ② 是商業教材掃描檔，而這個 repo 是公開的 GitHub Pages
# 只當本地參考來源用，永遠不要 commit（防止 `git add .` 誤傷）
assets/*.pdf

# 課堂逐字稿：內含 Owen 的家庭、工作、生活對話（家人病史、同事、住處等）
# 這個 repo 是公開的 GitHub Pages —— 逐字稿永遠只留本機，不 commit
transcripts/

# Anki 匯入檔：卡片內容含 Owen 的個人例句與教練註記，且真正的真相來源是 Anki 收藏檔本身
# （有 AnkiWeb 同步當備份）。這裡只當工作區，永遠只留本機
anki/

# 排程任務的報告：含 Owen 的學習數據與個人回答，repo 是公開的，只留本機
reports/

# 課本 PDF 抽出的純文字快取（tools/extract_textbook.py 產生）：一樣是商業教材內容，只留本機
assets/.textbook_cache.txt

# 掃描檔 OCR 出來的文字（tools/ocr_pdf.py 產生）：一樣是商業教材內容，只留本機
assets/.ocr_*.txt

# TCF 備考材料的本地工作區（2026-08-28 新增）：
# ① 第三方考試材料，而 repo 是公開的 GitHub Pages ② 體積達 GB 等級，遠超 GitHub 限制
# 只當本地參考來源用（校正考試題型與題目清單），永遠不要 commit
# 索引見 assets/tcf/INDEX.md（同樣只留本機）
assets/tcf/

# 用 Mac `say`（系統聲音）產生的音檔（2026-09-21）：
# macOS 授權第 2 節 F 項只准「個人、非商業」，錄製／發布／轉散布在「公開分享」情境不允許，
# 而這個 repo 是公開的 GitHub Pages → 這類音檔永遠只留本機（或私人空間），⛔ 不 commit
# 詳見 research/2026-09-21_預錄語音音檔的授權.md
audio/seg/
audio/sent/
audio/voice/
audio/t1_*.m4a
audio/樣本_*.m4a
