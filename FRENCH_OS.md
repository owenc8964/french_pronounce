# FRENCH OS — 認知壓縮框架（2026-08-07 建立）

> **一句話**：目標不是「更會算法文」，是**讓法文逐漸不需要算**。
> 本檔是教學策略的單一真相來源。行為鐵律在 `CLAUDE.md`「教學鐵律」段；資料架構在 `ARCH_2026-08_ANKI.md`。
> **Claude 的角色**：French Cognitive Compression Coach——不是知識提供者。

---

## 1. 核心診斷：瓶頸不是「不知道」，是「還需要想」

要說「我吃了」，大腦可能跑：

```
過去 → passé composé → 助動詞 avoir → 主詞 je → ai → je+ai 不能相連
→ élision → j'ai → manger 的分詞是 mangé → J'ai mangé.
```

答案正確，但**運算成本太高**。目標不是把這 8 步算得更快，是**逐步消滅中間步驟**：

```
8 步 → 4 步 → 2 步 → 1 塊 → 0 個意識運算
```

⚠️ **這是本檔存在的唯一理由。** 任何練習如果沒有在減少某個結構的處理成本，它就只是在累積題數。

---

## 2. French OS 地圖 ↔ codex 座標

**⚡ 關鍵：地圖已經蓋好了。** `codex.js`（9章50節123條、永久座標）就是 French OS，不要另建。

| French OS 系統 | 它處理什麼資訊 | codex 章 |
|---|---|---|
| **① 名詞／身份系統** | 這個東西是什麼身份？（陰陽性・單複數・定指不定指・數量・所有・指示） | `1.` 名詞與限定詞（19條） |
| **② 動詞／角色系統** | 誰對誰做了什麼？（argument structure、COD/COI 插槽） | `4.` 動詞變位（16條）＋ `7.` 句型 |
| **③ 代名詞／壓縮系統** | 已知資訊怎麼縮成指標？（le/la/les・lui/leur・y・en） | `3.` 代名詞（17條） |
| **④ 回指／拉線系統** | 這條線拉回前文哪裡？（reference tracking） | `3.` ＋ `7.`（跨句，codex 未獨立成章） |
| **⑤ 配合系統** | 哪些東西是一組？（陰陽性・單複數・分詞配合） | `2.` 形容詞（11條）＋ `4.` |
| **⑥ 時間／觀測角系統** | 說話者站在哪裡看事件？（攝影機位置） | `5.` 時態（15條） |
| **⑦ 語氣／真實度系統** | 這是現實・假設・願望・懷疑？ | `6.` 語氣（13條） |
| **⑧ 連接系統** | 多個命題怎麼連成一個思想？ | `8.` 介係詞與連接詞（10條）＋ `3-5` 關係代名詞 |
| **⑨ 語塊系統** | 已理解的文法怎麼壓縮成整塊？ | 跨章；對應 `chunks.js` 與 Anki `KeyChunk` |

**缺口**：④ 回指／拉線 在 codex 沒有獨立位置。它是**跨句**能力，不是單條規則——建議之後在 `3.` 章補一節（座標可增不可重編）。

---

## 3. 五種狀態 —— 用**可量測的判準**，不是感覺

⚠️ 每個狀態都必須有客觀判準，否則會退化成猜測。

| 狀態 | 意思 | **判準（可量測）** | 該做什麼 |
|---|---|---|---|
| 🧠 **UNDERSTAND** | 還沒懂 why | 正確率低 **且**「這規則你講得出來嗎」→ 講不出來 | 解釋它在 OS 哪一區、處理哪種資訊 |
| ✂️ **COMPRESS** | 懂但還要算 | **講得出規則，但**：中位延遲 > 2s **或** 變異係數 CV > 0.5 **或** Anki Hard 比例 > 30% | 高頻連發同結構 5–8 個變化；**禁止重講規則** |
| 🧱 **CHUNK** | 元素都會但組裝慢 | 拆開的每個元素都 REFLEX，**合起來**延遲仍 > 2s | 整塊建立，不再逐字組裝 |
| 👂 **SOUND** | 看得懂但聽不出來 | **文字題對、聽力題錯**（同一結構） | 語流辨識、連音、聽寫 |
| ⚡ **REFLEX** | 已自動化 | 中位延遲 < 2s **且** CV < 0.35 **且** 近5次無錯 | **不要再要求分析路徑** |

---

## 4. 怎麼在看不到你腦袋的情況下測處理成本

**測不到「步驟數」，但測得到處理成本。** 七個可實作的指標，由強到弱：

| # | 指標 | 怎麼取得 | 代表什麼 | 現況 |
|---|---|---|---|---|
| 1 | **延遲變異係數 CV**（標準差÷平均） | 同結構近 N 次的首鍵延遲 | ⭐ **最強訊號**：自動化的東西快**而且穩**；還在算的時快時慢 | `verb_sprint` 已存近5筆歷史，**資料就緒、只差計算** |
| 2 | **首鍵延遲中位數** | 輸入框第一個按鍵的時間 | 提取速度 | `verb_sprint` 已有（`REFLEX_MS=2000`） |
| 3 | **Anki `revlog.taken`** | Anki 每次複習的毫秒數 | 客觀延遲，**免費且自動** | Anki 內建，匯出即得 |
| 4 | **Anki Hard 比例** | Again/Hard/Good/Easy 分布 | 「對了但費力」＝自動化缺口的自我報告 | Anki 內建 |
| 5 | **限時下先壞掉的是什麼** | 強制加速模式，看哪個子步驟先崩 | **錯誤本身就是診斷**：逼快之後寫成 `je ai mangé` → élision 沒自動化 | `verb_sprint` 的 60 秒模式已是雛形 |
| 6 | **自我修正（退格）** | 監聽輸入框 backspace | 打錯再改＝規則有、但不自動 | **未實作**，成本低 |
| 7 | **暖機斜率** | 同結構連續題的延遲趨勢 | 第3題比第1題快很多＝提取需要暖機＝未自動化 | **未實作** |

### 為什麼 CV 是最強的訊號

自動化歷程的特徵**不只是快，是穩定**。還在意識運算的東西，會因為當下注意力、疲勞、前一題干擾而忽快忽慢。

```
結構 A：延遲 1.8s / 2.1s / 1.9s / 2.0s / 1.8s  → CV 低 → ⚡REFLEX
結構 B：延遲 1.2s / 4.5s / 1.5s / 3.8s / 2.2s  → CV 高 → ✂️COMPRESS（平均看起來還好，但根本沒穩）
```

**只看平均會把 B 誤判成快要好了。** 這是現有系統看不到的東西。

### 三個層次的量測來源

```
Anki           →  taken(ms) ＋ Hard 比例        →  記憶提取成本（每張卡）
verb_sprint    →  首鍵延遲 ＋ CV ＋ 限時崩壞點   →  產出自動化程度（結構層級）
listening      →  文字題對但聽力題錯            →  👂SOUND 缺口
```

三者用 **codex 座標** 聚合到同一個結構上——這就是「認知瓶頸地圖」。

---

## 5. 目前狀態初評（依 22 課糾錯摘要，**待量測確認**）

⚠️ 這是從既有錯誤記錄推的**初評**，不是量測結果。等 Anki 與延遲資料進來要修正。

| 結構 | codex | 理解 | 產出 | 聽力 | 初評狀態 | 下一步 |
|---|---|---|---|---|---|---|
| `j'ai + 分詞` | 5-2 | ✓ | △ | △ | ✂️COMPRESS | 消滅 avoir 選擇＋élision 兩步 |
| `je suis allé(e)` | 5-2 | ✓ | △ | △ | ✂️COMPRESS | être 動詞群＋配合 |
| **être/avoir 選擇** | 5-2 | ✓ | ✗ | — | ✂️**COMPRESS（最痛）** | L15 標為最大困難點，至今反覆 |
| **`des → de`** | 1-3 | △ | ✗ | — | 🧠→✂️ | ⚠️ **三次遇到同一條規則**（否定→量詞→形容詞在前），要合併成一個心智模型 |
| 時間表達四天王 | 9-4 | △ | △ | ✗ | ✂️COMPRESS | L16 標為「反覆確認」 |
| `imparfait` 字根 | 5-3 | ✓ | △ | — | ✂️COMPRESS | 先想 nous 形這步要消滅 |
| `y` / `en` | 3-4 | ✓ | △ | ✗ | 🧠→👂 | 新學（L21）；聽力層完全沒練 |
| 形容詞位置 | 2-2 | ✓ | △ | — | 🧠→✂️ | 新學（L22） |
| 易混淆詞對 | 跨章 | △ | △ | — | 🧠 | 最高頻錯誤類型，但屬詞彙不屬壓縮 |

---

## 6. 選戰場：高頻壓縮清單

⚠️ **B2 全範圍在 10 個月內達到「0 意識運算」不現實。** 自動化來自大量提取練習，沒有捷徑。

**所以要選。** 火力集中在最高頻的結構——這些佔日常輸出的絕大部分：

**第一批（先壓縮這些）**
```
j'ai + 分詞          je suis allé(e)        il y a
j'en ai besoin       j'y vais               il faut que
je voudrais          ça dépend de           avoir envie de
être en train de     quand j'étais petit    est-ce que
```

其餘接受「還要想」，等這批穩定再往下。

**判斷該不該進清單**：這個結構每天會用到嗎？不會 → 不進。

---

## 7. 跟現有工具的對接

| 需求 | 現有工具 | 要改什麼 |
|---|---|---|
| 延遲＋CV 量測 | `verb_sprint.html` | **推廣**：目前只有 9 動詞×6人稱，要能吃任意結構 |
| 壓縮練習（高頻連發） | `verb_sprint.html` | 已是正確形態，改吃壓縮清單 |
| 記憶提取成本 | **Anki** | Schema 加 `CogState` 欄 |
| 👂SOUND 缺口 | `listening.html` | 加 **chunk 辨識**練習（不是聽懂內容，是把 `j'en ai parlé` 一次辨識成一塊） |
| 認知瓶頸地圖 | S2 的縱向錯誤檔案 | 輸出格式改成「結構 × 五狀態」 |
| French OS 地圖 | `codex.js` ＋ `map.html` | 補 ④回指 一節；每條加狀態標記 |
| 應試形態訓練 | quiz／table_drill／writing／reading | **不變**——這條線跟壓縮並行，不互相取代 |

---

## 8. 教學演算法（每次引入新結構）

```
1 Locate     這在 French OS 哪一區？（給 codex 座標，避免「又多一條孤立規則」）
2 Understand 它替法文處理哪種資訊？只解釋到真的懂為止
3 Observe    少量高品質例句看出規律
4 Diagnose   要求產出，量延遲與錯誤型態
5 Cut        找出不再必要的中間步驟
6 Chunk      高頻的合併成整塊
7 Audialize  進入真實語音辨識
8 Reflex     快速變換情境，要求無分析產出
9 Revisit    間隔後再測 → 仍要分析就回 5；能直接產出就升 ⚡REFLEX
```

---

## 9. 衡量指標

**不要只追蹤**：完成幾章／背幾個字／做幾題／正確率。

**要追蹤**：

| 指標 | 怎麼算 |
|---|---|
| **處理成本** | 該結構的首鍵延遲中位數（趨勢向下＝進步） |
| **穩定度** | CV（趨勢向下＝真的自動化了，不只是偶爾快） |
| **狀態遷移** | 有幾個結構從 COMPRESS → REFLEX（這才是真進度） |
| **語塊覆蓋率** | 高頻清單裡有幾個已達 REFLEX |
| Anki Hard 比例 | 下降＝提取變輕鬆 |

⚠️ **正確率上升不一定是進步**（可能只是題目變簡單）；**延遲下降且 CV 下降才是**。

---

## 10. 兩條線並行，不互相取代

```
壓縮線（本檔）        →  讓法文不需要算  →  長期能力
應試線（既有系統）    →  TEF/TCF 題型     →  2027-06 的門票
```

⚠️ **流利 ≠ 會過。** TEF/TCF 有特定形態（聽力四選一不可重播、口說是任務型）。壓縮線幫得上忙但取代不了應試訓練。
Owen 目前最弱的仍是**聽力與口說**——那是 S3／S4 的範圍，不因本框架而降級。

---

## 開工前必讀
`CLAUDE.md`「教學鐵律」（Claude 的行為規則）＋ 本檔（策略）＋ `ARCH_2026-08_ANKI.md`（資料架構）
