# 強化計畫（2026-08-03 建立）

> 建立背景：第21課入庫後，Owen 要求以「B2 法語考試老師＋程式專家」視角全面檢視，並把所有待強化項目分類、分 session 進攻。
> 本檔是**待辦總表與 session 切分依據**。每個 session 開工前讀對應段落即可，不必讀完全檔。
> 定位區別：`SURVEY_2026-07.md` 是「程式健康度檢視報告」；本檔是「**要做什麼、誰先做**」的執行計畫。

---

## 分類 A — 本次新發現（教學法／考試觀點）

### A-1 ⭐ 易混淆詞對訓練器（最高投報率）
**證據**：抽出 21 課全部「本課糾錯摘要」的統計行後，跨課出現頻率**最高**的錯誤類型是易混淆詞對——bronze/bronzer、droit/drôle、chercher/trouver、séparer/divorcer、entraîneur/entraîner、voir/regarder、venir/revenir、odorat/odeur、paddle/stand-up paddle、le poste 雙義、mail 假朋友、marquer、enchanté 兩義、saveur/savait、bon 當語氣詞…**幾乎每課 2–3 對**。
**問題**：`quiz.html` 的 topic 按「知識領域」切（etre-avoir／articles／vocab-loisirs…），**沒有任何工具把兩個會混的東西並排逼你選**。這些對照現在只以散文形式躺在筆記的 note-box 裡，沒進題庫、沒進 SRS、沒進處方。
**做法**：收割既有筆記 → pair 題庫（中文＋情境，兩選項逼選，答錯給對照解說）。**不需要新素材**。
**規模**：預估可收 40–60 對。

### A-2 ⭐ 糾錯摘要 → 縱向錯誤檔案
**問題**：每課的「統計」行是**死的散文**（如「文法混淆 ×2（imparfait 字根要從 nous 形找、雙 i）」）。21 課累積的高品質人工診斷完全沒有被任何程式讀取。
**做法**：結構化成資料（課次／類型／具體項目）→ dashboard 讀得到 → 對**反覆出現**的類別在處方中自動加重。
**價值**：這是花錢買不到的個人化診斷資料，目前使用率 0%。

### A-3 L1（中文母語）加權缺失
**問題**：中文沒有冠詞、陰陽性、動詞變位、時態形態。所以文法點對 Owen 的難度**天差地遠**，但系統的處方給每個點同樣的曝光權重。
- **永久困難**（該永遠留在複習池）：冠詞／部分冠詞（`des→de`）、陰陽性一致、PC vs imparfait 的體貌
- **幾乎免費**（學會就會了）：SVO 語序、疑問詞、否定位置
**證據**：`des→de` 在第18、19 課糾錯摘要都出現，第19課註明「老師花時間逐步拆解句子結構才講清楚」。這不是偶發，是中文母語者的長期弱點。

### A-4 時間／空間定位能力被系統拆散
**證據**：第16課糾錯寫「pendant/longtemps/toujours/depuis 四者分工**反覆確認**是本課最大困難點」，同課還有「à+年紀 vs il y a vs depuis」；第20課是 imparfait 的時間副詞路標；第21課 y/en 本質是方向定位。
**問題**：老師眼中這是**同一種能力**（在時間軸／空間軸上定位事件），但系統切成 `duree`／`imparfait`／`pronoms-y-en` 三個互不相干的 topic 與三組 codex 座標。每次都在「重新學一次」而不是「把同一件事練熟」。

### A-5 ⭐⭐ 篇章層（discourse）工具缺失 —— 架構天花板
**核心判斷**：現有九項連動全部是**項目層**訓練（單字／變位／單句）。這對 A1→A2 極有效，成果也證明了。但 **B2 不是「更多項目」，是換一種能力**：60–90 秒有結構的論述、200 字以上有論點—論據—讓步—結論的文章、連接詞與語域控制。**再刷 3000 張卡也到不了。**
**訊號**：`writing_tasks.js` 停在第17課、Owen 已在第21課——項目層的九項連動每課自動跑一次沒漏，唯一的篇章層工具落後四課沒人發現。架構的重心說明了它在乎什麼。
**方向**：B1 開始要長**第二層**工具，不是把第一層做更大。

### A-6 writing_tasks.js 落後 4 課
第17課 → 需補到第21課。**同時**把題型從「寫2句」升級成**指定結構**（例：用 d'abord/ensuite/mais/donc 寫 80 字回憶段落）。第20、21課的 imparfait＋y/en 正好是敘事文骨架，材料已經夠。

### A-7 評量指標與考試指標不同構
**問題**：系統核心指標是**答對率**，但 CEFR 是 **can-do、標準參照**。
**後果（反直覺但重要）**：學越難的東西數字越難看——第21課 y/en 一定比第3課問候語錯得多。所以「正確率下降」可能正是進步訊號，而 dashboard 的 CLB 等級判定卡（正確率 ≥75% 且 ≥5 題次）會讀成退步。**指標與目標不同構，長期會誘導去做熟的、避開難的。**

### A-9 ✅ **已完成（08-04，S7）** 發音顆粒度：整段／單句／單字三層（Owen 08-03 提出）
> 成品：新增共用模組 `tts_reader.js`，掛在 `reading.html`（三層全上）與 `french_notes.html` 第21課平行閱讀（只上整段層，原因見 HANDOFF 08-04 記錄）。其餘 12 個各自實作 TTS 的頁面**沒有動**——要不要收攏先問 Owen。實作判斷與完整驗證記錄見 `HANDOFF.md`「08-04」段。
**需求原話**：「如果是整段文章，可以的話發音除了整段唸完，還有只念單句甚至單字的功能」。
**現況實測**：
- 全站 13 個頁面有 TTS（`SpeechSynthesisUtterance`），**但 `reading.html` 完全沒有**（grep 結果 0）——22 篇閱讀短文、包含新加的兩篇 A2，一個字都唸不出來。
- `french_notes.html` 的發音掛在 `.compare-table td.m/.f`、`.conj-row`、`.fr-ex`、`.phrase-list li`、`.verb-card`、`.verb-name span`——都是**單句／單格**層級。
- **整段連續朗讀：全站沒有。單字點讀：全站沒有。**

**三層規格**：
| 層級 | 現況 | 要做的 |
|---|---|---|
| 整段 | ❌ | 連續朗讀整篇，句與句之間有停頓；可暫停／續播 |
| 單句 | ⚠️ 部分 | `reading.html` 的段落要能逐句點讀；朗讀時最好高亮當前句 |
| 單字 | ❌ | 點任一個法文單字就唸該字（查發音用） |

**注意**：語音篩選邏輯全站已統一（Amélie/Thomas 優先 → premium/enhanced → 雲端語音 → fr-FR），新功能要沿用同一套，不要另寫一份。

### A-8 老師課堂法語＝真實口語語料庫，幾乎沒被收割
**證據**：8 課有 🎙 區塊。實測 `On est bon ?`、`Tu l'as fait ou non ?` 在筆記裡有、在 `chunks.js`／`questions.js` 都是 0（只有第20、21課的部分因為當時順手加了才進卡片）。
**價值**：B1–B2 開始考語域（registre）與口語省略。這批句子是老師真實輸出的口語語料，正好補課本教不到的部分，**素材已存在**。

---

## 分類 B — `SURVEY_2026-07.md` 已提出、尚未實作

> 這些不是新發現，但仍然有效且未動工。列在這裡是為了讓計畫**完整**，避免因為「講過了」而永遠不做。細部規格見 SURVEY 的 D 段。

| 編號 | 項目 | 備註 |
|---|---|---|
| B-1 | 聽力庫只有 10 篇／13.2 分鐘，且會無限重播（第11天起測的是記憶不是聽力）；無 `level` 欄位、無 SRS | SURVEY `0-1`／`0-4` |
| B-2 | ⭐ 提問練習（TEF Section A／TCF tâche 2／DELF 第2部分都考「向考官提問」，系統一題都沒有）；可自動客觀計分 | SURVEY `0-2`，**需 PRD** |
| B-3 | 說服論述＋4/3/2 流利度訓練 | SURVEY `0-3`，**需 PRD** |
| B-4 | 口說記錄面與評分面（`speaking.html` 只是日誌無評分；`RUBRICS.md` 三把口說尺沒有任何檔案實作） | |
| B-5 | 處方沒有時間預算（實際13步、110–145分鐘，已吃滿每日上限，`buildPrescription()` 沒有一行在算時間） | SURVEY A-1／A-2 |
| B-6 | `sentences.js` 有 83/146 句與 `chunks.js` 完全重複（兩套 SRS 練同一句） | |
| B-7 | ⚡快速測驗 247 題 dashboard 完全讀不到（`clb7_dq_*` 有真實資料但 dashboard 零引用） | |
| B-8 | 手寫筆記 6 個 `fr_*`／`vr_*` key 從不同步也不在備份裡（全系統最不可重生的內容） | |
| B-9 | 雲端縮水保護（三次推空都是「不完整 localStorage 整包蓋掉雲端」，`push()` 加 key 數縮水閘即可堵住機制本身） | 高優先，防災 |
| B-10 | Shadowing 素材該改綁「當天聽的那一篇」（現在綁 Extra Français／Peppa Pig，跟考試與聽力庫都無關） | |
| B-11 | 聽力素材擴充到 30 篇＋補齊完全沒有的公告／電話留言／簡短訪談三種題型 | 取材鐵律見 SURVEY（Audio Lingua／PFF 的 robots.txt 禁止程式抓取，流程是 Owen 挑片、Claude 出題） |
| B-12 | 📝 Dictée 聽寫工具（完全複用現有 166 句，不需新素材） | |
| B-13 | 4 個孤兒檔案處置（含 `index.html` 目前就是正式站首頁） | |

---

## 分類 C — 技術債／維運

| 編號 | 項目 | 證據 |
|---|---|---|
| C-1 | **preview 的 `.js` 快取反覆咬人**：08-02、08-03 兩次 session 都踩到（quiz 頁面 `BANK` 停在舊值）。可考慮 dev 用查詢字串或 cache-control 根治 | 兩次 HANDOFF 記錄 |
| C-2 | 生成腳本缺統一的「寫入後 parse 驗證」工具（08-02 因此產生 `},,` 語法錯誤；08-03 靠腳本內建驗證才避開） | |
| C-3 | `.claude/worktrees/` 有 4 份舊 repo 副本、佔 21MB，會污染全域 grep 結果 | 實測 |
| C-4 | 沒有資料匯出／備份機制（SRS 狀態、錯題史是最不可重生的資產；跟 B-8／B-9 同一族） | |

> ✅ **已查證非問題，不要再列**：`french_notes.html` 的表格鐵律 **100% 合規**（142 個 `<table>` 對 142 個 `.compare-table` div，grep 多出的 16 筆是 CSS 選擇器）。

---

## Session 切分（建議）

分組原則：**同一批檔案的改動盡量收在同一 session**（避免衝突）、**純內容 vs 動架構分開**（風險隔離）、**每個 session 都能獨立驗收**。

| # | Session 主題 | 涵蓋 | 動到的檔案 | 風險 | PRD |
|---|---|---|---|---|---|
| ~~S1~~ ✅ | 🌾 ~~收割既有內容~~（08-04完成） | A-1、A-8、A-6 | questions.js／chunks.js／writing_tasks.js／quiz.html | 低（純內容） | 否 |
| **S2** | 🧠 **診斷與處方智能化** | A-2、A-3、A-7、B-5、B-7 | dashboard.html／新資料檔 | 中（動處方邏輯） | 部分 |
| **S3** | 🎧 **聽力補強** | B-1、B-11、B-10、B-12 | listening.html／RESOURCES_AUDIO.md | 中 | Dictée 需 |
| **S4** | 🗣️ **口說補強** | B-2、B-3、B-4 | 新頁面＋新 key | 高（新頁面） | **是** |
| **S5** | 📝 **篇章層（B1+ 第二層）** | A-5、A-4 | writing_tasks.js／新工具／gram_rules.js | 中高 | **是** |
| **S6** | 🛡️ **資料安全與技術債** | B-9、B-8、B-6、B-13、C-1～C-4 | sync_supabase.js／雜項 | 中（碰同步，要格外小心） | 否 |
| ~~**S7**~~ ✅ | 🔊 **發音顆粒度三層** | A-9 | reading.html／french_notes.html／**新增 tts_reader.js** | 低中（UI 功能） | 否 |

### 建議順序與理由
1. **S1 先做** — 純收割、零架構風險、當天見效，直接打在最高頻失分點上。
2. **S6 緊接** — B-9 雲端縮水保護是**防災**，三次推空事故的根因還在。愈晚做風險愈久。
3. **S2** — 讓處方變聰明，後面所有內容才知道該怎麼分配曝光。
4. **S3／S5 並行可** — 檔案不重疊。
5. **S4 最後** — 規模最大、需要完整 PRD。

---

## 開工前必讀（給每個 session 的 Claude）
- 專案鐵律：`CLAUDE.md`（內容來源、codex 座標、筆記表格格式、**測試前切隔離 ROOM**）
- 現況與歷史：`HANDOFF.md`（尤其開頭的「交付前自動試跑原則」）
- 動到新頁面／新 key → 走 `CLAUDE.md` 的「新功能三步驟」先寫 PRD
- ⚠️ 測會寫 `clb7_*` 的功能前：`grep "var ROOM" sync_supabase.js` 現場確認 → 改 TEST 值 → 測完復原 → **下一步只能是 `preview_stop`** → grep 確認無殘留才 commit
