# CODEX_STYLE — 文法資料庫條目撰寫規範

> 2026-07-12 與 Owen 確立。適用 `codex.js` 全部條目。任何 session（含 Sonnet 派工）擴寫或修訂條目前必讀。
> Owen 的定位：條目=一篇維基式小文章，「見樹也有看熱鬧跟看門道」——頂部30秒看懂大意，逐段展開到連細紋都有。

## 房間佈局（固定順序，永不換——記憶宮殿的房型一致性）

| 段 | 欄位 | 開合 | 內容標準 |
|----|------|------|----------|
| 🎧 錨句 | `ex` | 常開 | 2句完整自然的法文句（fr+zh），先給體感 |
| 📖 規則 | `pts` `grid` `mnem` | 常開 | 見下方「規則段寫法」 |
| 🕐 使用時機 | `usage` | 收合 | 「什麼情境下選用它」逐條列，每條盡量帶例句 |
| ⚠️ 例外 | `exc` | 收合 | 完整列出，不挑著寫；每條說明例外的內容與範圍 |
| 🆚 易混淆 | `vs` | 收合 | 跟哪條座標搞混、判斷分界；可帶 `grid` 對照小表 |
| 💬 更多例句 | `more` | 收合 | 4-8句，情境多樣（肯定/否定/疑問/口語） |
| 🔗 相關 | `see` | 常顯 | 相關座標互連（不是易混淆，是「延伸閱讀」） |

## 欄位格式

```js
{ n:'1-3-1', lvl:'A1', name:'定冠詞', fr:"le / la / l' / les", brief:'一行摘要',
  ex:   [{fr:'完整句。', zh:'中文（可括號加一個重點）'}],           // 2句
  pts:  [{r:'規則敘述', fr:'示範', zh:'中文'}],                    // 每條規則帶自己的示範
  grid: {head:['','陽','陰','複'], rows:[['定冠詞','le','la','les']]},  // 選用：該給全表就給
  mnem: '口訣/比喻/諧音（唯一允許隨性的地方）',                    // 選用
  usage:[{r:'情境描述', fr:'例句', zh:'中文'}],                    // 選用但強烈建議
  exc:  ['例外敘述（含範圍與例子）'],
  vs:   [{n:'1-3-4', note:'判斷分界一句話', grid:{...對照表選用}}],
  more: [{fr:'…', zh:'…'}],                                       // 4-8句
  see:  ['1-3-5','7-2-3'],                                        // 相關座標
  topics:[...], gram:'...' }                                       // 不可動
```

## 規則段寫法（Owen 07-12 的批評就是針對這段）

1. **準確完整優先，不打折**。「太攏統或太隨性」是死罪：每個說法要具體到可以拿去對答案。
   - ❌「四張臉：le la l' les」（名片式，一問三不知）
   - ✅「le 用於陽性單數（le livre）、la 陰性單數（la table）、l' 母音或啞音h開頭（l'école, l'hôtel）、les 全部複數（les livres）——l' 和 les 不分陰陽」
2. **比喻與口訣一律放 `mnem`，不准混進 pts**。pts 是百科正文，mnem 是便利貼。
3. **有表格就給表格**（grid）：冠詞全表、變位全表、代名詞全表。表格是 Owen 的主食。
4. 每條 pts 的 fr 示範**優先用完整句**，至少是有意義的對照組。
5. 法文逐字驗證：變位、性數配合、重音符號。不確定的寧可換簡單的例句。
6. 例外（exc）要寫**範圍**：不只「l'eau 例外」，要寫「-eau 結尾通常陽性，但 l'eau（水）是陰性——最高頻的一個例外」。

## 鐵律（不可違反）

- **座標 n 永不改、永不重編、永不回收**。
- `lvl / name / fr / brief / topics / gram` 未經 Owen 同意不動。
- `vs` 與 `see` 的目標座標必須存在（寫完跑驗證腳本）。
- 渲染器向下相容：pts 可以是字串（舊格式），但新寫的一律用物件。
- 改完必跑：`node --check codex.js` ＋ 座標集合比對（見 HANDOFF 07-11 晚驗證方式）。

## 黃金範本

第 1-3 節（冠詞，6條）是 v3 完整深度範本；第 1 章其他條目是 v2 範本（先句後規，深度待補）。擴寫其他章時以 1-3 為準。
