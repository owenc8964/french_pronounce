# SURVEY 2026-07-28 — France CLB7 系統效能檢視 ＋ 聽力／口說補強路線圖

> 執行者：Claude（Opus 5）。
> **檢視對象是這套程式，不是使用者。** 每一條都問同一個問題：*這套系統作為「考過 CLB7 的機器」，哪裡出力不足、哪裡會在半路失效。*
> **本報告只診斷不動手**；低風險修正另行 commit（見「已修清單」），中高風險項目一律只寫建議等 Owen 決定。
> 證據：靜態讀碼（檔案:行號）＋雲端唯讀 GET（`clb7_sync`，`updated_at 2026-07-27T12:45Z`）。全程未執行任何寫入 `clb7_*` 的操作，ROOM 始終是正式值、未啟動 preview。

---

## 📌 三個最影響「達標能力」的系統缺陷

| # | 缺陷 | 系統症狀 | 風險 |
|---|------|----------|------|
| 1 | **聽力內容庫只有 13.2 分鐘音檔、30 題，且會無限重播** | 第 11 天起「聽力測驗」變成回想記過的答案，不再測聽力 | 高 |
| 2 | **口說完全沒有練習面與評分面** | 佔考試 25% 的科目，系統對它的支援是 0 | 高 |
| 3 | **處方輸出 13 步／110–145 分鐘，超出系統自己設定的 1.5–2h 預算** | 程式沒有配速機制，只會一直往清單加東西 | 中 |

第 1、2 點就是 Owen 說的「聽力口說明顯很弱」——下面 **D 段**是完整的補強路線圖與實作規格。

---

## A. 教學法／時間分配（程式面）

### A-1. 【風險：中】處方產生器沒有預算概念，只會加不會排

`dashboard.html:751-756` 的 `MIDDLE` 有 9 項，`:1009` 再把 `study` 展開成 `['gramread','study']`，加上頭尾固定三步：

```
warmup + (gramread+study, lesson_quiz, drill, sprint, review, sentence, answercard, listen, shadowing) + writing + reading = 13 步
```

依各頁自己的參數估時（`gram_trainer.html:153` ROUND_SIZE=6、`table_drill` ROUND_SIZE=6、`review` PACKET_SIZE=10、`sentence_drill` 5 新句、`answer_card` 3 新卡、影集精讀一遍 ≈ 一集長度…）：**合計 110–145 分鐘**。

系統自己在 `HANDOFF.md` 寫的每日目標是 1.5–2 小時。**也就是說處方產生器開出的量，等於或超過它自己的上限，而且它不知道這件事**——`buildPrescription()` 從頭到尾沒有任何一行在算時間，`stepDefs` 裡也沒有 `estMin` 這種欄位。

**這是純粹的程式缺陷，不是內容問題**：每次新增功能就往 `MIDDLE` push 一個 key（07-09 加 listen、07-16 加 answercard／shadowing／gramread／lesson_quiz），從來沒有任何機制把總量拉回來。

**建議規格（不自行實作）**：

1. 每個 `stepDefs` 項目加一個 `estMin` 欄位（純資料，零風險）。
2. `buildPrescription()` 加一個 `BUDGET_MIN`（例如 90），照優先序累加到超過預算就停，其餘步驟移到「⏭ 有餘力再做」區塊。
3. 優先序不用新資料——現成的就有：到期卡數（`chunkDueCount()`／`sentenceDueCount()`／`answerCardDueCount()`）、文法點階段（`gramStageOf`）、topic 正確率（`getTopicStats()`）。**到期的、階段低的、正確率差的先進今天的預算內。**

這樣處方就從「一張永遠做不完的清單」變成「一份配得起時間的處方」——這正是 HANDOFF 開頭寫的「大腦」該做的事，目前它只做了「顯示」沒做「取捨」。

### A-2. 【風險：中】700h 進度條只顯示數字，不回饋給處方

`dashboard.html:1186` 已經算出 `paceNeeded = (GOAL_H - totalH) / daysLeft`，但這個數字只被印在進度條旁邊，**沒有任何下游用途**：

- 不影響 `buildPrescription()` 開幾步；
- 不影響任何警報；
- 不區分「今天做了 20 分鐘」跟「今天做了 3 小時」，處方長得一模一樣。

一台合格的達標機器應該是**閉迴路**：配速落後 → 今天的處方縮短、集中在高效益步驟（到期卡＋弱 topic），讓它變得做得完；配速超前 → 才放行加值步驟。現在是開迴路，算完就丟。

**建議**：`paceNeeded` 接進 A-1 的 `BUDGET_MIN`，變成動態預算。這是把兩個既有數字接起來，不需要新資料。

### A-3. 【風險：中】四技能裡兩科沒有「產生分數」的路徑，dashboard 就無法用它們做判斷

`dashboard.html:1314` 的四技能卡，資料來源盤點：

| 技能 | 分數從哪來 | 有沒有趨勢 | 狀態 |
|------|-----------|-----------|------|
| 📖 閱讀 | `reading.html` 自動判分 `correct/total` | ✅ 週對比＋快照 | 健全 |
| ✍️ 寫作 | `writing.html:326` 解析「分數：X/10」＋內建 A2 尺 prompt | ✅ 週均分＋快照 | **機制健全**，只是排在第 12 步 |
| 🎧 聽力 | `listening.html:781` 測驗答對率寫 `comp` | ⚠️ 有數字，但題庫會重播（見 D-1） | 分數會失真 |
| 🗣️ 口說 | **無** | ❌ | **完全沒有** |

`speaking.html` 收的是：類型（家教／Duolingo／自練／其他）、分鐘數、錯誤次數、錯誤類型、逐字稿、備註——**沒有任何評分欄位**。[RUBRICS.md:29-42](RUBRICS.md:29) 寫好的 A1/A2、B1、B2 三把口說尺，`grep` 全 repo 只有 `writing.html`／`creed.js`／`writing_tasks.js` 引用，**口說尺沒有任何檔案實作**。

結果：CLB 等級判定（`dashboard.html` 用 quiz 正確率）、四技能卡、週趨勢——**全部無法對口說做出任何判斷**。一台以「四科都要 CLB7」為目標的機器，其中一科是完全的盲區。

**補法見 D 段。**

### A-4. 【風險：低】`sentences.js` 有 83/146 句（57%）與 `chunks.js` 完全相同，兩套 SRS 各記一份

node 比對兩題庫的法文（正規化去標點大小寫）：**146 句裡 83 句在 `chunks.js` 有一模一樣的卡**。

而且兩頁的呈現**逐行相同**——都是「顯示中文 → 想 → 翻開 → 顯示法文＋自動發音 → 自評」（[review.html:358-391](review.html:358) vs [sentence_drill.html:343-376](sentence_drill.html:343)）。

系統效能上的意義：**這 83 句佔掉兩個 SRS 池的排程額度，但只產生一份學習效果**，而且兩邊的間隔會互相打架（A 池剛排到 7 天後，B 池可能明天又考）。以每天新卡 10＋5 計，這是把約 15% 的卡片吞吐量花在重複上。

**建議**：`sentence_drill.html` 抽卡時用一個 `Set` 排除已存在於 `CHUNKS` 的法文句（改動小，保住「精選句一定會被練到」的初衷）。

### A-5. 【風險：低】`opinions` 佔位點誤鎖 5 題 ← **已修**

`gram_rules.js:362` 的 `opinions` 是 B1 未開課佔位點（`unlocked:false`），卻掛著 `connectors-pour-parceque`。`quiz.html:624-627` 的 `getPool()` 會排除「對應文法點未解鎖」的題，於是 5 題第 7 課的 pour／parce que／mais **從 07-16 起再也不會被抽到**（pour/parce que 在第 2、7、16 課都教過）。純資料標記失誤。

### A-6. 【風險：中】`futur-proche` 只鎖住一半，系統對它的熟練度沒有數據

07-16 依 Owen 說法把 `futur-proche` 設 `unlocked:false`，quiz 的 8 題被排除。但**同一個文法在別的池子沒鎖**：`chunks.js` 8 張、`sentences.js` 4 句含 futur proche 的卡照抽（如第 8 課「Demain soir, je vais finir la robe.」）。

系統效能上的問題不是「該不該鎖」，而是**鎖的作用域不一致**：只鎖住了會產生數據的那一半，沒鎖住不產生數據的那一半。結果是這個文法點照練、但系統對它一無所知。

**建議**：問一句「futur proche 現在算學過了嗎？」——是就 `unlocked:true`（一行），否就把 chunks/sentences 那 12 句也一併排除。不論哪個答案，作用域都該一致。

### A-7. 【風險：中】兩個文法點的打字題池不足一輪，07-24 的重複出題 bug 仍是活的

`gram_trainer.html:214` 的 `poolOf()` 只吃打字題（`!q.opts`），`ROUND_SIZE=6`。打字題 < 6 的點，每輪必然吃光整池：

| 文法點 | 打字題數 | 狀態 |
|--------|----------|------|
| `relatifs`（qui/que） | **3** | ❌ 必然重複 |
| `imparfait` | **5** | ❌ 必然重複 |
| `imperatif` | **6** | ⚠️ 剛好打平，洗牌後仍同一批 |
| `duree-temps` | 13 | ✅ 07-24 已補 |

07-24 只補了 durée，另外兩點沒補。**這是題庫容量問題，跟 D-1 的聽力題庫容量是同一類病**：練習系統的內容存量小於它的消耗速率。

**建議**：從 `french_notes.html` 第 15 課（imparfait 無人稱動詞）、第 16 課（qui/que）的既有例句各改寫 6–8 題打字題。屬內容生產（受內容鐵律約束：必須出自筆記），先確認再做。

### A-8. 【查證結果：沒問題】四套 SRS 參數一致

| | INTERVALS | 畢業判定 | 新卡上限 |
|---|---|---|---|
| `review.html:180-209` | [1,3,7,14,30] | `days≥3 && iv≥3` | 10/天 |
| `sentence_drill.html:180-208` | 同上 | 同上 | 5/天 |
| `answer_card.html:183-211` | 同上 | 同上 | 3/天 |

三者 `grade()` 逐行等價。`quiz.html` 是另一套 w/c 計數制，目的不同、不衝突。**這一項查完確認沒有互相打架。**

---

## B. 使用體驗／流程摩擦

### B-1. 【風險：低】`reading.html` 是唯一不吃 `?guided=1` 的處方步驟

10/13 頁有 guided 自動開始（`verb_sprint.html:620`、`review.html:501` 等）。兩頁 `grep guided` 為 0，但逐一讀過起始邏輯後只有一個是問題：

- `table_drill.html` — **沒問題**，`init()`（`:895-902`）本來就無條件 `startSession()`。
- `reading.html` — `DOMContentLoaded` 只做 `renderArticleList()`（`:789`），落在 20 篇清單。清單有 `✓ 分數` 標記（`:900`），所以是「多一個決策」不是「不知道做過沒」。

**建議**：補一行 guided，自動開第一篇沒做過的（比照 `listening.html:955-958`）。因為「自動挑哪一篇」是行為決定，先確認再做。

### B-2. 【風險：低】三個處方頁沒有懸浮回饋＝收不到問題回報

重新 grep 核對（HANDOFF 的 07-07 記載已過時）：

| 頁面 | 計時器 | 同步 | 懸浮回饋 | 判斷 |
|------|:---:|:---:|:---:|------|
| `table_drill.html` | ✅ | ✅ | ❌ | **該補**——處方步驟 |
| `reading.html` | ✅ | ✅ | ❌ | **該補**——處方步驟 |
| `verb_sprint.html` | ✅ | ✅ | ❌ | **該補**——處方步驟 |
| `french_basics.html` | ❌ | ❌ | ❌ | 筆記導覽列直連的真實練習工具，時間不進 700h |
| `verb_reference.html`／`map.html` | 部分 | 部分 | ❌ | 參考頁，可不補 |
| `speaking.html`／`tracker.html` | ❌ | ✅/❌ | ✅ | 日誌工具，刻意不計時，合理 |

系統效能上的意義：**回饋通道的覆蓋率決定系統的自我修正速度**。目前三個每天在用的練習頁沒有通道，那些頁面的問題只能靠口頭轉述。

### B-3. 【風險：低】錯題本缺答案卡的來源標籤 ← **已修**

`dashboard.html:1057` 的 `SRC` 對照表沒有 `answercard` key（`answer_card.html:423` 用 `src:'answercard'`），錯題本會顯示原始英文字串。

### B-4. 【風險：低】`table_drill.html` 的錯題進不了今日錯題本

`logWrong()` 有 7 份複本（quiz／reading／sprint／review／sentence_drill／answer_card／gram_trainer），`table_drill.html` 沒有。填表格正是最容易錯的機械性項目，它的錯題目前完全不進睡前錯題本。

**補之前先決定**：抄第 8 份，還是把 `logWrong()` 抽成共用的 `wrong_log.js`（見 C-2）。

### B-5. 【風險：中】4 個孤兒檔案的處置建議

| 檔案 | 實查內容 | 建議 |
|------|----------|------|
| `french_notes拷貝.html`（70KB） | 只有 lesson-1~3、14 個 unit；用法文詞抽樣比對，**內容 100% 已被現行筆記（95 unit）涵蓋** | **刪除**（git 有歷史可回溯） |
| `french_sounds.html`（370KB） | 母音/鼻母音/子音發音表，**370KB 裡 352KB 是單一個內嵌 base64 音檔**；內容是 `french_basics.html` 的子集 | **刪除** |
| `index.html`（16KB，**已被 git 追蹤**） | `french_sounds.html` 的無音檔版 | ⚠️ **它就是正式站根目錄首頁**——現在打開 `owenc8964.github.io/french_pronounce/` 看到的是一頁發音表，不是 dashboard。**建議改寫成入口／轉址頁** |
| `time_editor.html`（27KB） | 音檔時間軸剪輯工具，只被 `index.html` 連到 | **保留但搬進 `assets/tools/`**——做聽力素材時還會用到（見 D 段） |

**四項都是動檔案＝中高風險，不自行動手。**

### B-6. 【查證結果：沒問題】`french_notes.html` 結構

派工點名要驗證「懸浮回饋 script 是否重複兩份」——**沒有重複**：`<script>` 6 組／`</script>` 6 組配對正確、`#qnotePanel` 僅 1 次（`:6012`）、重複 id 0 個、lesson 區塊 19 個無重複。唯一同名的 `saveNotes`（`:5859` 與 `:6031`）**各自包在獨立 IIFE 裡**，作用域不相通，不是 bug。

`<table>` 123 個 vs `class="compare-table"` 123 個——**表格鐵律 100% 遵守**。

唯一結構隱憂：327KB／6232 行單檔、19 課同頁。現在沒有到痛的程度，記著等課次到 30+ 再拆。

### B-7. 【風險：中】dashboard 的資訊密度已經超過「一眼知道要做什麼」

系統每加一個功能就往 dashboard 加一張卡（session bar／13 步處方／📊完成率／錯題本／警報／CLB 判定／倒數／700h／四技能／週趨勢／Duolingo／🧭心法卡…），**從來沒有拿掉過任何一張**。結果「今天做什麼」跟「配速如何」這兩個真正驅動行動的資訊被埋在中段。

**建議**：頂部只留「session bar ＋ 今天預算內的步驟 ＋ 配速」，其餘收進可展開的「📊 詳細數據」。跟 A-1／A-2 是同一套改動。

### B-8. 【風險：中】⚡快速測驗（247 題）是 dashboard 完全看不到的第二套系統

`quiz.html:386` 的 `⚡ 快速測驗` 背後是完整的第二套題庫與 SRS：`AGREE_BANK` **247 題**（`questions.js:996`）、加權出題（`:1145`）、複習輪、弱點分析、自己的 SRS key `clb7_dq_*`。

**雲端實查：`clb7_dq_*` 有 64 筆答題記錄，這套東西真的被用過。**

但 `grep clb7_dq dashboard.html` = **0**。dashboard 的 `getTopicStats()`／`qPct`／CLB 等級判定**只讀 `BANK`**——這 247 題的表現對「今日處方」「最弱 topic」「CLB 判定」零影響。

照 [CLAUDE.md](CLAUDE.md) 自己的「大腦檢查」（數據 dashboard 讀得到嗎？能影響今日處方嗎？）——**這是全系統最典型的「兩個都不能」**。`AGREE_BANK` 已經有 `topic` 欄位（`questions.js:1376-1409`），接進統計不難。

---

## C. 技術健康度

### C-1. 【風險：中，最高優先】比「備份」更根本的是讓事故不會發生

雲端被推空三次（07-06／07-16／07-20），三次機制完全相同：`sync_supabase.js:77-83` 的 `push()` 是把當下分頁的 `collect()` **整包 upsert 覆蓋**，合併只發生在 `pull()`。所以任何 localStorage 不完整的分頁推一次，就蓋掉完整雲端。**備份只讓你救得回來，不能讓它不發生。**

**① 縮水保護（最該做，改動最小）**——push 前先看雲端 key 數，明顯縮水就拒推：

```js
function push() {
  var mine = collect();
  return fetch(URL + '/rest/v1/clb7_sync?id=eq.' + ROOM + '&select=payload', { headers: H })
    .then(function (r) { return r.ok ? r.json() : []; })
    .then(function (x) {
      var remote = (x[0] && x[0].payload) || {};
      var rn = Object.keys(remote).length, mn = Object.keys(mine).length;
      if (rn >= 20 && mn < rn * 0.5) { console.warn('[ClbSync] 拒推：本機 ' + mn + ' key < 雲端 ' + rn + ' 的一半'); return; }
      return fetch(URL + '/rest/v1/clb7_sync', { method:'POST',
        headers: Object.assign({}, H, { Prefer:'resolution=merge-duplicates' }),
        body: JSON.stringify({ id: ROOM, payload: mine, updated_at: new Date().toISOString() }) });
    }).catch(function () {});
}
```

為什麼安全：`pull()` 開頁就跑且合併回本機，正常裝置的 localStorage 一定是雲端的超集，永遠不會觸發這道閘。會被擋下的正是三次事故那種「幾乎空白的 preview 分頁」。代價：每次 push 多一個 GET。

**② repo 內每日快照（零程式碼）**：session 開場 curl 一份存進 `backups/cloud-YYYY-MM-DD.json`，一份約 300–400KB，留最近 30 天。

**③ Supabase 第二張表**：功能最完整但要新增資料表，非必要，放最後。

⚠️ `sync_supabase.js` 每一行都影響真實資料，**即使低風險我也不自行動手**。

### C-2. 【風險：中】三支卡片頁 75–84% 是同一份程式碼——但現在不建議重構

`difflib` 逐行比對主 script：

| 比對 | 行數 | 相同 | 重疊率 |
|------|------|------|--------|
| review ↔ sentence_drill | 325 / 310 | 247 | **80%** |
| review ↔ answer_card | 325 / 330 | 245 | **75%** |
| sentence_drill ↔ answer_card | 310 / 330 | 259 | **84%** |

成本已經發生過：07-11 加🔴標記要改兩份、07-16 建 answer_card 是整份複製。下次改 SRS 演算法就是三份一起改。

**但不建議現在動**：MVP 精神明講「三行重複優於提早的抽象」；A-8 查證過三者參數一致、沒有任何實際 bug 由它造成；抽共用檔要動三個每天在用的頁面，風險/報酬不划算。

**真正該做的是便宜版**：只把 `logWrong()`（7 份複本，HANDOFF 自己標記「格式要四處同步」為地雷）抽成 `wrong_log.js`。這樣 B-4 也順便從「抄第 8 份」變成「加一行 script」。

其餘重複規模供參考：`todayStr` 9 份、`shuffle` 6 份、TTS 語音挑選 6 份、`flushTime` 6 份、懸浮回饋三件套各 10–11 份。

### C-3. 【風險：中】手寫筆記的 6 個 key 完全不同步、也不在任何備份裡

`sync_supabase.js:22` 只收 `clb7_` 開頭的 key。但系統有 6 個不是這個前綴、且存的全是手打內容：

| key | 寫入者 | 內容 |
|-----|--------|------|
| `fr_unit_notes` / `fr_li_notes` | `french_notes.html` | unit 筆記 / 逐句行內筆記 |
| `fr_card_notes` / `fr_sound_notes` / `fr_tbl_notes` | `french_basics.html` | 發音卡／音節／文法表筆記 |
| `vr_verb_notes` | `verb_reference.html` | 動詞表筆記 |

後果：①換裝置看不到 ②雲端 payload 查無此物（已實查）③一旦本機出事就真的沒了。

**而這是全系統最不可重新生成的內容**——題庫、卡片、筆記本體我都能重做，手寫的那句「這裡我一直搞混」不能。

**兩種修法**（都要先確認）：A 改名成 `clb7_notes_*` ＋一次性搬遷；B `sync_supabase.js:22` 的條件同時收 `fr_`／`vr_` 前綴（一行，但命名更亂）。

### C-4. 【查證結果】沒有死 key，但有 3 個「大腦看不到」的

掃過全部 `.html`／`.js` 的讀寫配對：**每個 `clb7_*` 都至少被自己那頁讀回，沒有純死 key。**

「只有自己讀得到」的三組：`clb7_dq_*`（64 筆真實資料，見 B-8）、`clb7_drill_sessions`／`clb7_drill_flags`、`clb7_notes_marks`。

另外雲端顯示這些 key 從未被建立：`clb7_creed_own`、`clb7_order_lock`、`clb7_flagged_qs`、`clb7_ac_upgrade_ready`、`clb7_notes_marks`。**其中兩個牽涉到 HANDOFF 要求「Claude 每次 session 要檢查」的儀式**（🚩檢舉清單、Answer Card 升級待辦）——清單一直是空的，這兩條 session 儀式目前是空轉，可以從 HANDOFF 拿掉或改成「有才處理」。

### C-5. 【風險：中】第 17 課課文的版權處理比第 19 課寬鬆，建議往嚴的靠

| 課次 | 來源 | 處理 | 段落內法文句（≥20字） |
|------|------|------|----------------------|
| 第 17 課 | *Cosmopolitan*（2021-06-24，已標出處） | 逐句法中對照 | **約 30 句** |
| 第 19 課 | CIJF 訪談（2019-05-21，已標出處） | 摘要＋短引用為主 | 13 句 |

HANDOFF 07-26 記錄了這個轉向（[HANDOFF.md:512](HANDOFF.md:512)「之後比照這次的做法」），但沒回頭處理第 17 課。兩篇都有標出處，情況不算糟，但正式站是**公開的 GitHub Pages**。

**建議照第 19 課模式改寫**：保留全部文法示範句（passé composé 三種助動詞情境正是該課重點，一句不少）＋中文摘要，刪掉純敘事段落。屬內容改寫，不自行動手。

---

# D. 聽力／口說補強路線圖

> 這一段是 Owen 點名要研究的部分。先量現況容量，再對照考試規格，然後排進度。

## D-1. 現況：聽力子系統的容量與失效點

`listening.html` 的 `LISTENING_BANK` 實測拆解：

| 類型 | 篇數 | 題數 | 音檔總長 | 中位長度 |
|------|------|------|----------|----------|
| 真人語速（`audioUrl`） | **10** | 30 | **794 秒 ＝ 13.2 分鐘** | 43 秒 |
| TTS（`script`） | 8 | 24 | — | — |

**四個結構性問題：**

**① 題庫會無限重播，第 11 天起就不再測聽力。**
`listening.html:955-958` 的 guided 邏輯是 `LISTENING_BANK.find(x => x.audioUrl && !listenQuizDoneToday(x.id))`——**只檢查「今天」做過沒**。10 篇全部做過一輪之後，每天都會從 `r01` 重新開始，而且**每篇的 3 題永遠是同樣的 3 題**。第二輪起答對率會虛高，`comp` 這個欄位就失去意義，四技能卡的聽力數字跟著失真。

**② 沒有難度欄位，做不出等級進程。**
每筆的欄位是 `id, lesson, title, done_key, audioUrl, durationSec, sourceUrl, sourceLabel, questions`——**沒有 `level`**。所以系統無法表達「這篇是 A2、那篇是 B1」，也就無法隨 CLB_STAGE 推進難度。而且 10 篇全部標 `lesson:15`（是入庫日期的痕跡，不是真的難度）。

**③ 題型跟考試題型不同構。**
10 篇全是「商店交易對話」（麵包店／車站／市場／肉店／魚店／藥局／問路／起司店／咖啡廳／郵局），全部是三選一選擇題。TEF/TCF 聽力實際包含的**廣播公告、電話留言、訪談、較長論述**，系統裡一篇都沒有。

**④ 沒有 SRS，答錯的篇章不會回來。**
`clb7_listening` 只存 `{date, source, min, comp, notes, quizId}`，沒有逐題對錯、沒有到期時間。聽錯的內容不會被重新排程——這是全系統唯一沒有 SRS 的練習類型。

## D-2. 現況：口說子系統

**練習面：不存在。** 沒有任何頁面提供口說練習動作。`dashboard.html:762-763` 的階段文案寫著「口說盲說 2 分鐘」，但 `grep 盲說` 全 repo 只有這兩行——**沒有對應功能**。

**記錄面：`speaking.html` 只是日誌。** 欄位＝類型／分鐘數／錯誤次數／錯誤類型／逐字稿／備註。沒有評分，沒有題目，沒有計時，不在處方裡，沒有 `session_timer.js`。

**評分面：尺有了，沒人用。** [RUBRICS.md:29-42](RUBRICS.md:29) 的三把口說尺寫得很完整（A1/A2 尺：講滿目標秒數就給基本分、只挑 2 個錯、優先序動詞>冠詞>介係詞），但**沒有任何檔案實作它**。

**對照考試需求**（[ROADMAP.md:76-83](ROADMAP.md:76) 老師課堂實測）：口說三部分裡有兩部分是**可以完全提前準備**的——第 2 部分抽字卡**向考官提問**（字卡池約 15 主題，老師說是固定輪替）、第 3 部分角色扮演（情境池固定）。

**這裡有個系統設計上的重要落差**：`answer_cards.js` 準備的是「**回答**問題」（對應第 1 部分 Entretien dirigé），而第 2 部分要的是「**提出**問題」——系統目前完全沒有練提問。老師親口說字卡池是固定的，這是全系統投報率最高的一塊，卻是空的。

## D-3. 補強路線圖（四階段，跟 CLB_STAGE 對齊）

設計原則，每一項都先過 [CLAUDE.md](CLAUDE.md) 的大腦檢查：**dashboard 讀得到嗎？能影響今日處方嗎？**

### 階段 0（現在就該補的基礎建設，A2 期間）

| # | 要做的事 | 為什麼是它 | dashboard 讀得到 | 影響處方 |
|---|---------|-----------|:---:|:---:|
| **0-1** | **聽力題庫加 `level` 欄位＋輪播改成「未做過優先、全做完才進第二輪並換題」** | 直接修掉 D-1①②，讓聽力分數重新有意義 | ✅ 現有 `comp` | ✅ 現有步驟 |
| **0-2** | **聽力 SRS**：逐題對錯寫進 `clb7_listen_srs`，答錯的篇章 1/3/7 天後重排 | 聽力是唯一沒有 SRS 的練習類型；引擎現成（三支卡片頁已驗證） | ✅ 新增到期數 | ✅ 到期優先 |
| **0-3** | **🎙️ 盲說 2 分鐘（`speaking.html` 內建）**：計時 → 手機鍵盤內建聽寫直接打進 textarea → 一鍵複製含 RUBRICS A2 口說尺的 prompt → 貼回分數 | 完全複刻 `writing.html` 已驗證的迴路，**零新基礎建設**；把 RUBRICS 的口說尺從文件變成程式 | ✅ 新 `score` 欄位進四技能卡 | ✅ 新步驟 |
| **0-4** | **🃏 字卡提問練習**：抽一張主題卡 → 60 秒內想 3 個問句 → 打字/聽寫 → 對照範例問句 | 老師說字卡池固定輪替、是最能提前準備的部分；系統目前完全沒有 | ✅ 可計答題數 | ✅ 併入口說步驟輪替 |

**0-3 的技術路徑選擇（研究過的結論）**：不用 Web Speech API。iOS/Android 鍵盤內建的聽寫按鈕可以直接把語音打進任何 `<textarea>`，跨瀏覽器、零相容性風險、零 API 成本，而且產出的就是要送去評分的文字。`webkitSpeechRecognition` 在 iOS Safari 的支援歷來不穩，不值得為它冒險。**先用鍵盤聽寫，之後若要自動化再考慮。**

### 階段 1（A2 後半）——把聽力庫從 13 分鐘擴到能撐住每日一篇

- **內容擴充目標：真人音檔 10 篇 → 30 篇**，並補齊三種目前完全沒有的題型：**廣播/店內公告、電話語音留言、簡短訪談**。
- **來源**：`podcastfrancaisfacile.com`（前面 session 已實測過音檔可 embed、逐字稿公開；版權原則沿用 HANDOFF 07-09 確立的：音檔串流不下載、題目一律原創、逐字稿連結出去不入 repo）。
- **每篇標 `level`**（A2／A2+／B1），讓 0-1 的輪播可以隨階段推進。
- **📝 聽寫（Dictée）工具**——HANDOFF「下一步」第 3 條掛了很久：TTS 唸 `sentences.js` 的句子 → 打出來 → 逐字比對標紅。**這是投報率最高的聽力工具**：它強迫由下而上解碼（聽清楚每個字），跟選擇題的「猜大意」互補，而且**內容完全複用現有的 146 句，不需要新素材**。

### 階段 2（B1）——進考試題型

- **RFI Journal en français facile**：每日 10 分鐘新聞、有逐字稿，是 A2→B1 過渡公認素材（HANDOFF 07-07 已查證，因機器人防護改用官方 embed）。
- **聽力測驗改成 TCF 形態**：一次 5–8 題連續作答、只聽兩次、不看逐字稿作答（現在是單篇 3 題、可無限重播）。
- **口說**：盲說從 2 分鐘 → TEF section A（連續提問 8 題）／section B（表態說服）題型；每週一次完整計時演練。
- **InnerFrench 解鎖**（HANDOFF 記載等 B1 前後）。

### 階段 3（B2）——全真

- RFI 正常語速、四科全真計時模擬每兩週一次。
- 口說進 italki 真人（ROADMAP 已列，是全計畫唯一要花錢的訓練）。
- 系統角色轉成**記分板**：模擬考成績趨勢 → 哪科最低就往哪科倒資源（ROADMAP B1 進場動作已寫）。

## D-4. 建議的動手順序（我的推薦）

**第一批（一次做完，彼此相關）：0-1 ＋ 0-3。**

理由：
- **0-1 是修復**——聽力步驟現在每天在跑，但從第 11 天起它測的是記憶不是聽力。修這個立刻讓一個既有步驟恢復效力，成本最低。
- **0-3 是補洞**——四科裡唯一的盲區，而且完全複刻 `writing.html` 已經驗證過的迴路（複製 prompt → 貼回分數 → 解析 → 四技能卡 → 週趨勢），不需要發明任何新機制。
- 兩者做完，四技能卡才第一次**四科都有真實分數**，CLB 判定才能從「只看 quiz 正確率」升級成真正的四科判定。

**第二批：0-2 ＋ 0-4。** 第三批：階段 1 的內容擴充與聽寫工具。

⚠️ 依 [CLAUDE.md](CLAUDE.md) 的新功能三步驟，0-3／0-4 會動到新頁面／新資料 key，**要先寫 PRD 問清楚**（誰用、流程與狀態、必填欄位、完成後看到什麼）再動手。上面這份是研究與規格草案，**還不是 PRD，也還沒有寫任何一行程式**。

---

## ✅ 已完成的低風險修正（各自獨立 commit）

| commit | 內容 | 驗證 |
|--------|------|------|
| `8195270` | **A-5**：清空 `gram_rules.js` 的 `opinions` 佔位點 `topics` | node 模擬 `getPool()`：該 topic 可出題數 **0→5**；`GRAM_POINTS` 仍 32 點、`futur-proche` 維持鎖住未被波及；`node --check` 通過；確認 `map.html:792`／`codex.js` 對空 topics 的處理跟其他 12 個佔位點一致 |
| `fb63a48` | **B-3**：`dashboard.html` 錯題本 `SRC` 補 `answercard:'🗣️ 答案卡'` | grep 確認與 `answer_card.html:423` 的 `src` 值一致 |

**全程未啟動 preview、未執行任何寫入 `clb7_*` 的操作**，ROOM 從頭到尾是正式值（commit 前已 grep，repo 無 `TEST-DO-NOT-USE` 殘留）。對雲端只做唯讀 GET。

---

## 🙋 需要決定的清單

**聽力／口說（D 段，建議優先）**

1. **先做 0-1 ＋ 0-3 嗎？**（修好聽力輪播失效 ＋ 補上口說評分迴路）——我的推薦。
2. 0-3 的口說輸入方式確認用「手機鍵盤內建聽寫打進 textarea」嗎？（避開 Web Speech API 的相容性風險）
3. 0-4 字卡提問練習要不要做？主題池用老師課堂看到的 8 張＋補到 15？
4. 階段 1 的聽力擴充（10 → 30 篇、補公告/留言/訪談三種題型）要排嗎？
5. 聽寫（Dictée）工具要不要做？（複用現有 146 句，不需新素材）

**系統結構（A/B/C 段）**

6. 處方要不要加時間預算與動態配速？（A-1／A-2）
7. 造句練習排除跟複習卡重複的 83 句？（A-4）
8. futur proche 算學過了嗎？（A-6，不論答案哪個，鎖的作用域都該一致）
9. `relatifs`／`imparfait` 打字題池補到 8 題以上？（A-7）
10. 雲端縮水保護要不要做？（C-1，比備份更根本）
11. 手寫筆記的 6 個 `fr_*` key 要不要納入同步備份？（C-3）
12. ⚡快速測驗 247 題接進 dashboard 統計，還是明確定位成支線？（B-8）
13. 4 個孤兒檔案照 B-5 處置嗎？（特別是 `index.html` 目前是正式站首頁）
14. `table_drill` 錯題進錯題本 ＋ `logWrong` 抽共用檔？（B-4／C-2）
15. `reading.html` 補 guided？三個處方頁補懸浮回饋？（B-1／B-2）
16. 第 17 課課文改成摘要＋短引用？（C-5）
17. 🚩檢舉清單／Answer Card 升級待辦這兩條 session 儀式（清單從未被建立過）要保留還是拿掉？（C-4）
