# SURVEY 2026-07-28 — France CLB7 系統性檢視報告

> 執行者：Claude（Opus 5）。範圍照派工 prompt 的 A/B/C 三面向。
> **本報告只診斷不動手**；低風險修正另行 commit（見文末「已修清單」），中高風險項目一律只列在這裡等 Owen 決定。
> 證據來源：靜態讀碼（檔案:行號）＋ **Owen 真實雲端資料**（唯讀 GET `clb7_sync`，`updated_at 2026-07-27T12:45Z`，372 個 key）。全程未執行任何寫入 `clb7_*` 的操作，ROOM 始終是正式值、未啟動 preview。

---

## 📌 先看這一頁：三個最該處理的事

| # | 發現 | 證據 | 風險等級 |
|---|------|------|----------|
| 1 | **700h 進度已落後約 2 倍**，且處方步數（13 步）遠超每日 1.5–2h 的預算 | 雲端 `clb7_tracker` 累計 25.2h／24 個日曆天；剩 308 天需 **2.19h/天不休息** | 中（要 Owen 決定砍哪些步驟） |
| 2 | **輸出兩科（寫作、口說）近 30 天完成率 0/15**，但 ROADMAP A2 要求輸出佔 40% | 雲端無 `clb7_writing`／`clb7_speaking` 任何一筆 | 中 |
| 3 | **雲端沒有備份**，已被推空三次全靠運氣自我修復 | HANDOFF 07-06／07-16／07-20 三則事故 | 中（方案見 C-1） |

---

## A. 教學法／時間分配

### A-1. 【風險：中】處方從 7 步長到 13 步，沒有人重新算過時間預算

`dashboard.html:751-756` 的 `MIDDLE` 陣列現在有 9 項，`dashboard.html:1009` 又把 `study` 展開成 `['gramread','study']`，加上頭尾固定的 warmup／writing／reading：

```
warmup + (gramread+study, lesson_quiz, drill, sprint, review, sentence, answercard, listen, shadowing) + writing + reading = 13 步
```

**HANDOFF 從頭到尾都寫「9步番号」**（[HANDOFF.md:115](HANDOFF.md:115)），系統狀態表也是 9 步——這份文件已經跟程式脫節兩輪（07-16 加 answercard／shadowing／gramread／lesson_quiz 時各加一步，沒回頭改總數）。

逐步保守估時（依各頁實際參數推算，非憑感覺）：

| 步驟 | 依據 | 估時 |
|------|------|------|
| ①🔥熱身 Quiz 5題 | `quiz.html` warmup | 3 min |
| 📚文法閱讀 1 條 | codex 條目已是「維基式小文章」 | 4–6 min |
| 📐文法路徑 6 題打字＋包尾重試 | `gram_trainer.html:153` `ROUND_SIZE=6` | 8–10 min |
| 📚研讀＋專項Quiz（挑一課） | 含「開筆記研讀」，無上限 | 15–25 min |
| 📝填表格 6 表＋複習輪 | `table_drill.html` `ROUND_SIZE=6` | 10–12 min |
| ⚡動詞衝刺 | 60 秒＋熱機 | 3 min |
| 📦複習卡 10 張 | 頁面自報 3–5 min | 5 min |
| 🗣️造句 5 新句＋到期 | `sentence_drill.html:181` | 8–10 min |
| 🗣️Answer Card 3 新＋到期 | `answer_card.html:184` | 8–10 min |
| 🎧聽力真人語速 1 篇 | 音檔 12–36 秒＋3 題＋核對 | 6–8 min |
| 🎬影集精讀 1 遍 | 一集 Extra Français ≈ 12 分鐘 | 15–20 min |
| ✍️寫作 1 則＋貼給 Claude 批改 | `writing.html` 複製 prompt 迴路 | 15 min |
| 📖閱讀 1 篇 | | 8 min |
| **合計** | | **約 110–145 min** |

也就是說：**全部做完＝已經吃掉 1.8–2.4 小時，等於每日目標的上限甚至超出**，中間沒有任何緩衝，而且這還沒有算 ROADMAP A2 階段要求的「口說盲說 2 分鐘、每週 3 次」。

**真實資料佐證**（雲端 `clb7_tracker` type=`session`，15 個活躍日）：

| 步驟 | 近 15 個活躍日完成 | 步驟 | 近 15 個活躍日完成 |
|------|------|------|------|
| ①🔥熱身 | **13/15** | 🗣️造句背誦 | 6/15 |
| 📦複習卡 | 7/15 | ✍️寫作 | **0/15** |
| ⚡動詞衝刺 | 6/15 | 🗣️口說日誌 | **0/15** |
| 📚研讀+Quiz | 4/15 | 🎧聽力 | 3/15 |
| 📐文法路徑 | 3/15 | 📚文法閱讀 | 3/15 |
| 📖閱讀 | 2/15 | 📝填表格 | 2/15 |
| 🗣️Answer Card | 1/15 | 🎬影集精讀 | 1/15 |

只有第①步穩定被做完（87%），**其餘 12 步中位數約 20%**。這不是紀律問題，是**處方開得比一天裝得下的還多**——步驟一多，實際行為就退化成「做前面一兩步就散掉」。

**建議（需 Owen 拍板，不自行動手）**：把 13 步拆成「**每日必做核心 5 步**（熱身／文法路徑＋閱讀配對／複習卡／造句／輸出一則）＋**輪替加值 2 步**」，讓一天 7 步、約 60–75 分鐘可完成，剩餘時間留給輸出與通勤聽力。現行 `getMiddleOrder()` 已經是輪替架構，只要把 `MIDDLE` 拆成兩層即可，改動很小；但**改處方步數屬於方向決定，照派工要求不自行實作**。

### A-2. 【風險：中】700h 目標已落後約 2 倍，dashboard 有算但沒有把落後講白

- 雲端實際：`clb7_tracker` 49 筆、有效時數 **25.2h**（session 25.0h＋auto-* 0.2h），最早 2026/07/04、最新 2026/07/27。
- 日曆天 24 天 → 實際 **1.05 h/天**；扣掉沒開機的日子，活躍日 16 天平均 1.6h（跟目標一致），**問題出在活躍率只有 2/3**。
- 距 2027-06-01 尚餘 308 天，`(700 − 25.2) / 308 =` **2.19 h/天且一天不休**。

`dashboard.html:1186` 的 `paceNeeded` 已經算出這個數字，但它只是被塞進進度條旁的文字；**沒有任何警報把「照現在速度會差多少」講明**。這是「大腦」應該最先喊的那一句話，現在反而最安靜。

**建議**：700h 卡加一行紅字對比「需要 2.19h/天 ｜ 你最近 14 天平均 1.05h/天」。低風險，但因為牽涉到「700h 這個目標本身要不要調」（也可能結論是砍目標而不是加時數），列為**待 Owen 決定**。

### A-3. 【風險：中】輸出兩科（寫作／口說）實際完成率 0，但 A2 階段要求輸出 40%

- 雲端 payload **完全沒有 `clb7_writing` 這個 key**，也沒有 `clb7_speaking`——不是分數低，是**一筆都沒有**。
- ROADMAP A2 要求：寫作升級 TCF tâche 1、**每週 3 篇下限**；口說盲說 2 分鐘、**每週 3 次**。
- 系統現況：
  - 寫作：`writing.html` 迴路完整（依課出題→複製 prompt 含 A2 尺→貼回自動解析「分數：X/10」→ `dashboard.html:1306` 週均分＋趨勢＋週快照）。**機制是好的，只是沒被使用**——它排在 13 步的第 12 位，前面 11 步做不完就永遠輪不到它。
  - 口說：**完全沒有練習步驟**。`speaking.html` 只記 `min`／`errCount`／逐字稿，**沒有任何評分欄位**；[RUBRICS.md:29-34](RUBRICS.md:29) 的 A1/A2 口說尺**沒有任何檔案實作**（`grep` 只有 `writing.html`／`creed.js`／`writing_tasks.js` 提到 RUBRICS）；「盲說」兩個字在整個 repo 只出現在 `dashboard.html:762-763` 的階段文案裡，沒有對應功能。

HANDOFF「產出能力評估的落差」那一欄（[HANDOFF.md:75](HANDOFF.md:75)）說寫作口說都沒有評分機制——**這句話對寫作已經過時**（07-12 的 RUBRICS＋writing.html 已補齊），**對口說完全正確**。

**這個缺口有多嚴重**：口說佔 DELF/TEF 四科的 25%，而且是老師明講「一定要一起做、自己對螢幕練不出來」的那一科（[ROADMAP.md:87](ROADMAP.md:87)）。目前系統對它的支援是零。**值得優先補**，但補法有兩條路（① speaking.html 照 writing.html 抄一套「錄音→轉文字→貼 prompt→回填分數」迴路 ② 排一個獨立的「盲說 2 分鐘」步驟進處方），屬於新功能方向決定，**列給 Owen 選**。

### A-4. 【風險：低】`sentences.js` 有 83/146（57%）跟 `chunks.js` 是同一句，兩套 SRS 各記一份

用 node 比對兩個題庫的法文（正規化去標點大小寫）：**146 句裡有 83 句在 `chunks.js` 找得到一模一樣的卡**。

而且 `review.html` 跟 `sentence_drill.html` 的**呈現方式完全相同**——都是「顯示中文 → 想 → 翻開 → 顯示法文＋自動發音 → 自評對錯」（比對 [review.html:358-391](review.html:358) 與 [sentence_drill.html:343-376](sentence_drill.html:343)，兩段程式碼除了變數命名幾乎逐行一致）。

也就是說：**這 83 句 Owen 每天可能練兩次，用兩個獨立的 SRS 記錄，練法一模一樣，沒有任何增益**——而且會互相污染間隔判斷（在 A 系統剛答對排到 7 天後，B 系統可能明天又考一次）。

原始設計意圖是好的（`sentences.js` 是人工精選、`chunks.js` 是自動抽取），但**精選的結果幾乎是抽取結果的子集**，設計上的區隔在資料層沒有成立。

**建議（風險低但影響題庫，仍列給 Owen 確認）**：`sentence_drill.html` 抽卡時排除「法文已存在於 `CHUNKS`」的句子，或反過來讓 `review.html` 跳過已被 `sentences.js` 收錄的卡。前者改動小（一個 `Set` 過濾），且保住「精選句一定會被練到」的初衷。

### A-5. 【風險：低】`opinions` 文法點鎖住，害第 7 課 5 題永遠不會出現 ← **已修，見文末**

`gram_rules.js:362`：

```js
{ id:'opinions', ..., zone:'B1', lessons:[], topics:['connectors-pour-parceque'], unlocked:false },
```

這是一個 **B1 未開課佔位點**，但它「認領」了 `connectors-pour-parceque` 這個 topic。而 `quiz.html:624-627` 的 `getPool()` 會把「對應文法點 `unlocked:false`」的題目整批排除——所以 `questions.js` 裡 5 題**第 7 課**的 pour／parce que／mais 題目**從 07-16 起就再也不會被抽到**。

而 pour/parce que 在筆記裡是第 2、7、16 課都教過的內容（`french_notes.html` 三課共出現 12 次）。這是純粹的資料標記失誤，不是設計決定。

### A-6. 【風險：中】`futur-proche` 被鎖在 quiz，但複習卡／造句每天照練 12 句

07-16 因為 Owen 說「還沒學到未來式」，把 `futur-proche` 設成 `unlocked:false`（[HANDOFF.md:329](HANDOFF.md:329)），quiz 的 8 題被排除。

但**同樣的文法在別的池子沒有被鎖**：`chunks.js` 有 8 張、`sentences.js` 有 4 句含 futur proche 的卡（如第 8 課「Demain soir, je vais finir la robe.」「La semaine prochaine, nous allons tricoter un pull.」），複習卡與造句練習每天照抽。

也就是說這個「鎖」只鎖住了**能追蹤正確率的那一半**（quiz），沒鎖住**不追蹤的那一半**（背誦卡）——結果是：Owen 照樣在背 futur proche 的句子，但系統對他的 futur proche 熟練度沒有任何數據。

再者，`french_notes.html` 第 4／8／10／15／17／18 課都出現過 futur proche，第 8 課是完整教學單元。**「還沒學到」這個判斷很可能已經過期。**

**建議**：問 Owen 一句「futur proche（je vais + 原形）現在算學過了嗎？」——答「是」就 `unlocked:true`（一行），答「否」則應該把 chunks/sentences 裡那 12 句也一併排除，不能只鎖一半。**這是狀態判斷不是程式問題，不自行更動。**

### A-7. 【風險：中】三個文法點的打字題池仍然太薄，07-24 那個「連兩天出同題目」的 bug 只修好了一個

`gram_trainer.html:214-217` 的 `poolOf()` 只吃打字題（`!q.opts`），`ROUND_SIZE=6`（`gram_trainer.html:153`）。打字題數 < 6 的點，**每次進去都是同一批題**，就是 Owen 07-24 回報的症狀。

現況（用 node 對 `BANK` 實測）：

| 文法點 | 打字題數 | 狀態 |
|--------|----------|------|
| `relatifs`（qui/que） | **3** | ❌ 一輪 6 題吃不滿，必然重複 |
| `imparfait` | **5** | ❌ 同上 |
| `imperatif` | **6** | ⚠️ 剛好打平，洗牌後仍是同一批 |
| `duree-temps` | 13 | ✅ 07-24 已補 |

HANDOFF 07-11 就列過這份「題池偏薄」清單（[HANDOFF.md:298](HANDOFF.md:298)），07-24 只補了 durée（[HANDOFF.md:500](HANDOFF.md:500)），**另外兩個到今天還沒補**——bug 對 relatifs／imparfait 仍然是活的。

**為什麼不自己補**：補題＝寫新的法文題目，受 [CLAUDE.md](CLAUDE.md) 內容鐵律約束（必須出自 `french_notes.html` 或課程材料）。可以做，但要逐題回筆記取材、屬於內容生產不是 bug 修復，**列為建議動作**：從 `french_notes.html` 第 15 課（imparfait 無人稱動詞單元）與第 16 課（qui/que 單元）的既有例句改寫，各補 6–8 題打字題即可解除。

### A-8. 【風險：低】四套 SRS 參數其實是一致的（這項查完沒問題）

派工要求核對 quiz／review／sentence_drill／answer_card 的 SRS 是否打架。實測結果：

| | INTERVALS | 畢業判定 | 新卡上限 | 包尾重試 |
|---|---|---|---|---|
| `review.html:180-209` | [1,3,7,14,30] | `days≥3 && iv≥3` | 10/天 | ✅ |
| `sentence_drill.html:180-208` | 同上 | 同上 | 5/天 | ✅ |
| `answer_card.html:183-211` | 同上 | 同上 | 3/天 | ✅ |

三者的 `grade()` 邏輯逐行等價（`answer_card.html:416` 用 `if (first)`、另兩支用 `if(!first){}else`，語意相同）。`quiz.html` 是另一套 w/c 計數制，跟卡片 SRS 目的不同，不衝突。**這一項沒有問題**，但正好證明 C-2 的重複程式碼問題。

新卡總負荷：10＋5＋3 = **每天 18 張新卡**＋quiz 題目。以 1040 張 chunks 計，10/天需 104 天走完一輪，在 308 天的死線內合理。

---

## B. 使用體驗／流程摩擦

### B-1. 【風險：低】`reading.html` 是唯一不吃 `?guided=1` 的處方步驟

`dashboard.html` 的 13 個步驟全部帶 `?guided=1`，10 頁有對應處理（載入即自動開始），例如：

```
verb_sprint.html:620   if (guided==='1') startSprint();
review.html:501        if (guided==='1') startPacket();
sentence_drill / answer_card / gram_trainer / listening / writing / quiz … 皆有
```

`table_drill.html` 與 `reading.html` 兩支 `grep guided` 都是 0 次，但**逐一讀過起始邏輯後只有一個是問題**：

- `table_drill.html` — **沒問題**。它的 `init()`（`table_drill.html:895-902`）本來就無條件呼叫 `startSession()`，開頁即開練，`guided` 參數只是多餘不是失效。
- `reading.html` — **確有摩擦**。`DOMContentLoaded` 只做 `renderArticleList()`（`reading.html:789`），落在 20 篇文章清單上要自己挑。清單有 `✓ 分數` 標記做過的篇（`reading.html:900`），所以摩擦是「多一個決策」而不是「不知道做過沒」，程度比原本想的輕。

閱讀排在處方第 13 步（最後一步），真實完成率 2/15——**排序位置的影響遠大於這一個點擊**（見 A-1）。

**建議**：`reading.html` 補一行 guided 處理，自動開啟第一篇沒做過的文章（比照 `listening.html` guided 的做法）。因為「自動挑哪一篇」是行為決定不是 bug 修復，**列給 Owen 確認再做**。

### B-2. 【風險：低】三支 script 的頁面覆蓋跟 HANDOFF「注意事項」記載已經對不上

實測（`grep -l`）對照 [HANDOFF.md:665-666](HANDOFF.md:665) 的 07-07 現況表：

| 元件 | 現況覆蓋 | HANDOFF 說法 | 差異 |
|------|----------|--------------|------|
| `session_timer.js` | 13 頁（新增 answer_card／gram_trainer／map） | 「共 9 頁」 | **已過時** |
| `sync_supabase.js` | 14 頁（多 tracker） | 未列表 | — |
| 懸浮回饋 | 11 頁 | 「共 9 頁」 | **已過時** |

**真正的缺口（現在仍然缺）**：

| 頁面 | 計時器 | 同步 | 懸浮回饋 | 判斷 |
|------|:---:|:---:|:---:|------|
| `table_drill.html` | ✅ | ✅ | ❌ | **該補**——它是每日處方第⑤步，是唯一「在處方裡卻收不到回饋」的練習頁 |
| `reading.html` | ✅ | ✅ | ❌ | **該補**——同上，處方第⑬步 |
| `verb_sprint.html` | ✅ | ✅ | ❌ | **該補**——處方步驟 |
| `french_basics.html` | ❌ | ❌ | ❌ | 07-11 就發現（[HANDOFF.md:643](HANDOFF.md:643)），Owen 說先不用。它是 `french_notes.html` 導覽列直連的真實練習工具，練的時間不算進 700h |
| `verb_reference.html` | ❌ | ❌ | ❌ | 參考頁，不在處方，可不補 |
| `speaking.html`／`tracker.html` | ❌ | ✅/❌ | ✅ | 日誌工具，刻意不計時，合理 |

補懸浮回饋是純加法（貼既有 snippet），屬低風險；但因為要動三個檔案的 HTML 尾段、且 Owen 沒點名要，**列為建議不自行動手**（避免「順手加東西」違反 MVP 精神）。

### B-3. 【風險：低】答案卡的錯題在今日錯題本裡顯示成英文 key ← **已修，見文末**

`dashboard.html:1057`：

```js
const SRC = { quiz:'🃏 Quiz', sprint:'⚡ 衝刺', reading:'📖 閱讀', review:'📦 複習卡', sentence:'🗣️ 造句練習', gram:'📐 文法路徑' };
```

`answer_card.html:428` 寫入錯題時用 `src:'answercard'`，**SRC 對照表沒有這個 key**，`SRC[e.src]||e.src` 於是 fallback 成原始字串，錯題本會顯示「answercard」而不是「🗣️ 答案卡」。HANDOFF 07-16 記錄說這項驗證過（[HANDOFF.md:361](HANDOFF.md:361)），但對照表確實沒補上。

### B-4. 【風險：低】`table_drill.html` 的錯題仍然收不進今日錯題本

`grep "function logWrong"`：quiz／reading／sprint／review／sentence_drill／answer_card／gram_trainer **共 7 頁有**，`table_drill.html` **沒有**。

這是 [HANDOFF.md:638](HANDOFF.md:638)「下一步」第 4 條，從 07-11 掛到現在。影響：填表格答錯的冠詞／變位不會出現在睡前掃的錯題本裡，而填表格正是最容易錯的機械性項目。

**為什麼列在報告而不直接修**：`logWrong()` 在 7 個頁面各有一份複本（見 C-2），要嘛再抄第 8 份、要嘛先把它抽成共用檔——後者是重構，屬中風險。**先問 Owen 要哪一種再動手。**

### B-5. 【風險：中】4 個孤兒檔案盤點結果與處置建議

派工要求給明確建議，不要「待決定」。逐檔查完的結論：

| 檔案 | 內容實查 | 建議 | 理由 |
|------|----------|------|------|
| `french_notes拷貝.html`（70KB, 1349 行, 5/23） | 只有 **lesson-1~3**、14 個 unit；用法文詞抽樣比對，**14 個 unit 的內容 100% 都在現行 `french_notes.html`（95 個 unit）裡找得到**，4 個「標題只在拷貝檔出現」的 unit（職業/商店/所有格/家人）內容也都已被現行檔涵蓋 | **刪除** | 純舊備份，零獨有內容，且 git 已有完整歷史可回溯 |
| `french_sounds.html`（370KB, 5/7） | `<title>Sons du Français</title>`，內容＝母音/鼻母音/子音發音表，**370KB 裡有 352KB 是單一個內嵌 base64 音檔** | **刪除** | 內容是 `french_basics.html` 的子集（後者同樣有這三節，另加所有格/冠詞/文法表）；352KB base64 是 repo 裡最大的無用負擔 |
| `index.html`（16KB, 5/7, **已被 git 追蹤**） | 跟 `french_sounds.html` 同一份頁面的無音檔版（diff 僅 250 行、差在 base64 與少數 CSS） | **改成入口頁或刪除** | ⚠️ 它是 GitHub Pages 的 `index.html`——現在網站根目錄打開會看到一頁發音表，而不是 dashboard。**建議改寫成一頁轉址／入口連結到 `dashboard.html`**，這比刪掉有價值 |
| `time_editor.html`（27KB, 5/7） | `<h1>🎙 Time Editor</h1>`，音檔時間軸剪輯小工具（23 處 audio/waveform/trim），只被 `index.html` 連到 | **保留但移進 `assets/tools/`** | 是做教材音檔時的一次性工具，跟學習流程無關；不刪是因為之後做 podcast/聽力素材可能還會用到 |

**四個都屬「刪除／搬移檔案」＝中高風險，照派工要求不自行動手。** 特別是 `index.html` 牽涉正式站首頁行為，一定要 Owen 點頭。

### B-6. 【風險：低】`french_notes.html` 結構檢查——已知可疑點驗證結果：**沒有重複**

派工點名要驗證「懸浮回饋 script 區塊有沒有重複兩份」。實查結果：

- `<script>` 6 組 / `</script>` 6 組，配對正確；`<style>` 2 組配對正確。
- **重複 id：0 個**；lesson 區塊 19 個無重複。
- `#qnotePanel` 只出現 1 次（`french_notes.html:6012`），懸浮回饋 script 只有 1 份（6025–6077）。
- 唯一的「重複」是 `saveNotes` 這個函式名被定義兩次（`:5859` 與 `:6031`），但**兩者各自包在獨立的 IIFE 裡**（`(function(){…})()`），作用域不相通，**不是 bug**。

`<table>` 123 個、`class="compare-table"` 123 個，**完全對齊**——CLAUDE.md 表格鐵律第①條目前 100% 遵守。

**唯一真正的結構問題**：檔案 327KB／6232 行單檔，19 課全部塞在一頁。這會讓每次開筆記都載入 19 課的 DOM。**目前還沒有到痛的程度（Owen 沒抱怨過慢），不建議現在動**——記在這裡是為了讓之後課次到 30+ 時知道要拆。

### B-7. 【風險：中】從「打開 dashboard」到「開始有效練習」的決策點盤點

實際走一次（讀碼推演，未開 preview）：

1. 開 `dashboard.html` → 頁面同時渲染 session bar、13 步處方、📊完成率、今日錯題本、警報、CLB 判定、倒數、700h、四技能、週趨勢、Duolingo、🧭心法卡 —— **一屏放不下，處方要往下捲**。
2. 按「開始今日學習」（啟動計時）。
3. 在 13 步裡找「下一步」高亮。
4. 點進去 → 10/13 的頁面自動開始（好），2 個掉在選單（B-1），1 個（研讀+Quiz）刻意讓 Owen 選課（設計決定，[HANDOFF.md:653](HANDOFF.md:653) 明列「不強迫選哪課」）。

**摩擦最大的不是點擊數，是第 1 步的資訊密度**：dashboard 現在有 12 個以上的區塊，而「今天該做什麼」跟「我落後多少」這兩件真正驅動行動的資訊，被埋在同一頁的中段。這跟 A-1 的 13 步是同一個病：**這個系統每加一個功能就往 dashboard 加一張卡，沒有人拿掉過任何一張。**

**建議**：dashboard 頂部只留「session bar ＋ 今天的 3 件必做 ＋ 落後多少」，其餘卡片收進可展開的「📊 詳細數據」區。這是版面重構，**屬中風險，只列不做。**

### B-8. 【風險：中】⚡快速測驗（247 題配合練習）是一整套 dashboard 完全看不到的系統

`quiz.html:386` 有一個 `⚡ 快速測驗` 模式按鈕，背後是完整的第二套題庫與 SRS：

- 題庫：`questions.js:996` 的 `AGREE_BANK`，**247 題**（冠詞/性別配合類）
- 出題：`quiz.html:1145` `weightedSample(AGREE_BANK, DRILL_TOTAL=20)`，有加權、有複習輪、有 `renderDrillWeakAnalysis()` 弱點分析
- 資料：自己的 SRS key `clb7_dq_*`（`questions.js:1412` `drillQId()`）、`clb7_drill_sessions`、`clb7_drill_flags`

**雲端實查：Owen 真的在用它——`clb7_dq_*` 有 64 筆答題記錄。**

但是：
- `grep clb7_dq dashboard.html` → **0 次**。dashboard 的 `getTopicStats()`／`qPct`／CLB 等級判定**全部只讀 `BANK`**，這 247 題的正確率對「今日處方」「最弱 topic」「CLB 判定」**零影響**。
- 它不在 13 步處方裡，沒有任何入口從 dashboard 指過去。

照 [CLAUDE.md](CLAUDE.md) 自己訂的「新功能先過大腦檢查」（數據 dashboard 讀得到嗎？能影響今日處方嗎？兩個都不能就先確認做它的意義）——**這是全系統最典型的一個「兩個都不能」案例**，而且不是新功能，是既有的 247 題在裸奔。

**建議**：不要刪（有真實使用記錄）。兩條路擇一：① 把 `clb7_dq_*` 併進 dashboard 的 topic 統計（`AGREE_BANK` 已經有 `topic` 欄位，`questions.js:1376-1409` 有標記邏輯，接得起來）② 或者承認它是「隨手練」的支線，明確不納入統計。**屬方向決定，列給 Owen。**

---

## C. 技術健康度

### C-1. 【風險：中，最高優先】雲端沒有備份，而且「備份」不是最該做的那件事

HANDOFF「下一步」第一條（[HANDOFF.md:598](HANDOFF.md:598)）：雲端被推空三次（07-06／07-16／07-20），三次都靠 Owen 真機一開自我修復。

**先講一個比備份更根本的觀察**：三次事故的機制完全相同——`sync_supabase.js:77-83` 的 `push()` 是把當下分頁的 `collect()` **整包 upsert 覆蓋**，而 `pull()` 才有合併邏輯。所以任何「localStorage 不完整的分頁」在任何時機推一次，就會把完整的雲端蓋掉。**備份只是讓你救得回來，不能讓事故不發生。**

建議三層，優先序如下：

**① 縮水保護（最該做，改動最小，直接堵住事故機制）**

`push()` 之前先看一眼雲端現有的 key 數，如果這次要推的明顯縮水就拒推：

```js
function push() {
  var mine = collect();
  return fetch(URL + '/rest/v1/clb7_sync?id=eq.' + ROOM + '&select=payload', { headers: H })
    .then(function (r) { return r.ok ? r.json() : []; })
    .then(function (x) {
      var remote = (x[0] && x[0].payload) || {};
      var rn = Object.keys(remote).length, mn = Object.keys(mine).length;
      // 雲端有量、而本機不到雲端一半 → 這包不完整，不准覆蓋
      if (rn >= 20 && mn < rn * 0.5) { console.warn('[ClbSync] 拒絕推送：本機 ' + mn + ' key < 雲端 ' + rn + ' 的一半'); return; }
      return fetch(URL + '/rest/v1/clb7_sync', { method:'POST', headers: Object.assign({}, H, { Prefer:'resolution=merge-duplicates' }),
        body: JSON.stringify({ id: ROOM, payload: mine, updated_at: new Date().toISOString() }) });
    }).catch(function () {});
}
```

- 為什麼安全：`pull()` 在開頁時就跑了、而且是合併回本機，所以正常裝置的 localStorage 一定是雲端的超集，永遠不會觸發這道閘。真正會被擋下的，正是三次事故那種「幾乎空白的 preview 分頁」。
- 代價：每次 push 多一個 GET（debounce 700ms，量很小）。
- ⚠️ **這支檔案的每一行都會影響 Owen 真實資料，即使是低風險修改我也不自行動手**，等他點頭。

**② repo 內每日快照（零程式碼、Claude 可執行）**

每次 session 開場（或收尾）多跑一行，把雲端現況存進 repo：

```bash
curl -s -H "apikey: $KEY" -H "Authorization: Bearer $KEY" \
  "https://hgkqyrglftljxaieberm.supabase.co/rest/v1/clb7_sync?id=eq.owen-clb7-k9f3a72q&select=payload,updated_at" \
  > "backups/cloud-$(date +%F).json"
```

一份約 300–400KB，一天一份、只留最近 30 天，一年也才 10MB 級。git 已經 19MB，可以吸收。**這件事我今天就可以做第一份**，但因為要新增 `backups/` 目錄與 commit 習慣，仍先問 Owen。

**③ Supabase 第二張表**（`clb7_backup`，每天第一次開站寫一份）——功能最完整但要新增資料表與程式碼，**放最後，非必要**。

### C-2. 【風險：中】三支卡片頁 75–84% 是同一份程式碼

`review.html` / `sentence_drill.html` / `answer_card.html` 的主 script 逐行比對（`difflib`）：

| 比對 | 行數 | 逐行相同 | 重疊率 |
|------|------|----------|--------|
| review ↔ sentence_drill | 325 / 310 | 247 行 | **80%** |
| review ↔ answer_card | 325 / 330 | 245 行 | **75%** |
| sentence_drill ↔ answer_card | 310 / 330 | 259 行 | **84%** |

三份各自維護的同名函式：`isGraduated` / `saveSrs` / `newCountToday` / `startPacket` / `renderCard` / `flip` / `grade` / `renderFlagBadge` / `saveFlags` / `pickVoice` / `renderStart`…（共 11 個以上）。

實際成本已經發生過：07-11 加🔴手動標記時要改兩份、07-16 建 answer_card 時是**整份複製**（[HANDOFF.md:357](HANDOFF.md:357) 自己寫「完全複刻 sentence_drill.html 的引擎」）。下次要動 SRS 演算法就是三份一起改，漏一份就是靜默不一致。

**但這裡有個更重要的判斷：不建議現在重構。**

- MVP 精神（CLAUDE.md）明講「三行重複的程式碼優於一個提早的抽象」；
- 這三支都在正常運作，A-8 查證過參數一致，**沒有任何實際 bug 由它造成**；
- 抽共用檔要動三個正在每天用的頁面，風險/報酬比不划算。

**真正該做的是更便宜的版本**：只把**跨頁複製最嚴重、且格式必須四處同步**的 `logWrong()` 抽成一支 `wrong_log.js`（7 個檔案各一份複本，[HANDOFF.md:669](HANDOFF.md:669) 自己標記「格式要四處同步」為地雷）。這樣 B-4 的 table_drill 補錯題本也順便從「抄第 8 份」變成「加一行 script」。**屬中風險重構，只列不做。**

其餘跨頁重複（供 Owen 判斷規模）：`todayStr` 9 份、`logWrong` 7 份、`shuffle` 6 份、`initVoice`/TTS 語音挑選 6 份、`flushTime` 6 份、懸浮回饋 `loadNotes/saveNotes/updateCount` 各 10–11 份。

### C-3. 【風險：中】Owen 手寫的筆記**完全沒有同步、也不在任何備份裡**

`sync_supabase.js:22` 只收集 `clb7_` 開頭的 key：

```js
if (k && k.indexOf('clb7_') === 0 && k !== 'clb7_session') d[k] = localStorage.getItem(k);
```

但系統裡有 6 個**不是** `clb7_` 開頭的 key，存的全是 Owen 自己打的字：

| key | 寫入者 | 內容 |
|-----|--------|------|
| `fr_unit_notes` | `french_notes.html` | 每個 unit 標題旁「✏️筆記」按鈕存的筆記 |
| `fr_li_notes` | `french_notes.html` | 逐句 phrase-list 的行內筆記 |
| `fr_card_notes` / `fr_sound_notes` / `fr_tbl_notes` | `french_basics.html` | 發音卡／音節／文法表的筆記 |
| `vr_verb_notes` | `verb_reference.html` | 動詞表筆記 |

後果三重：**①換裝置看不到 ②雲端 payload 裡查無此物（已實查確認）③三次推空事故如果發生在本機而不是雲端，這些字就真的沒了**。

而這正是全系統**最不可重新生成**的內容——題庫、卡片、筆記本體 Claude 都能重做，Owen 自己在第 13 課旁邊寫的那句「這裡我一直搞混」不能。

**修法有兩種**，都要 Owen 選：
- **A（乾淨）**：改名成 `clb7_notes_unit` 等，寫一段一次性搬遷（讀舊 key → 寫新 key → 刪舊 key），之後自動納入同步。
- **B（最小）**：`sync_supabase.js:22` 的條件改成同時收 `fr_`／`vr_` 前綴。一行改動，但會讓 key 命名更亂。

⚠️ 兩種都會動到 `sync_supabase.js` 或搬遷真實資料，**絕不自行動手**。

### C-4. 【風險：低】死 key／孤立資料盤點結果

掃過全部 `.html`／`.js` 的 `localStorage` 讀寫配對，結論：

**✅ 沒有「寫了完全沒人讀」的死 key。** 每個 `clb7_*` 都至少被自己那頁讀回。

但有 3 個「**只有自己讀得到、大腦讀不到**」的：

| key | 寫入 | 讀取 | 問題 |
|-----|------|------|------|
| `clb7_dq_*`（64 筆真實資料） | `quiz.html` 快速測驗 | `quiz.html` 弱點分析 | dashboard 完全看不到，見 B-8 |
| `clb7_drill_sessions` / `clb7_drill_flags` | `quiz.html` | `quiz.html` | 同上；雲端目前無此二 key（表示 Owen 練過題但沒完成整輪 20 題） |
| `clb7_notes_marks` | `french_notes.html` | `french_notes.html` 重新套用 `<mark>` | 只能靠「複製標記給 Claude」人工傳遞；雲端目前無此 key（＝Owen 還沒用過選字標記） |

另外雲端 payload 顯示這些 key **從來沒有被建立過**：`clb7_writing`、`clb7_speaking`、`clb7_creed_own`、`clb7_order_lock`、`clb7_flagged_qs`、`clb7_ac_upgrade_ready`、`clb7_notes_marks`。前兩個是 A-3 的輸出缺口；後五個代表這些功能（自寫心法、鎖定順序、🚩檢舉、答案卡升級待辦、選字標記）**上線以來一次都沒被使用過**。

這不必然是壞事（有些是保險絲），但值得跟 Owen 對一次：**做了沒人用的功能，跟沒做一樣佔維護成本**。尤其🚩檢舉機制（[HANDOFF.md:584](HANDOFF.md:584) 要求「Claude 每次 session 要檢查這份清單」）——**清單從來是空的，這條 session 儀式可以刪掉**。

### C-5. 【風險：低】`clb7_ac_upgrade_ready` 的維護儀式同樣是空轉

同上：HANDOFF 要求每次 session 檢查 Answer Card 升級待辦。實查雲端：**key 不存在**，且 `clb7_answercard_sessions` 只有 1 筆（07-16 上線當天）。

Answer Card 15 張卡、新卡上限 3/天，**Owen 至今只做過一次**。這套系統（`answer_cards.js` + `answer_card.html` 595 行）目前的實際投報率是全系統最低的——但它同時是老師親口肯定、且直接對應 DELF 口說第 1／2 部分的東西（[ROADMAP.md:81](ROADMAP.md:81)）。

**這不是「刪掉它」的訊號，是「它排在第 9 步所以永遠輪不到」的訊號**——跟 A-1 是同一個病根。

### C-6. 【風險：中】第 17 課課文的版權處理方式跟第 19 課不一致，建議往嚴的那邊靠

派工要求核對「引用課本以外的真實文章是否為摘要＋短引用」。實查兩篇：

| 課次 | 來源 | 處理方式 | 段落內法文句（≥20 字） |
|------|------|----------|------------------------|
| 第 17 課 | *Cosmopolitan*（2021-06-24，已標出處） | **逐句法中對照全文** | **約 30 句** |
| 第 19 課 | CIJF 訪談（2019-05-21，已標出處） | 摘要為主＋短引用，主篇幅是文法示範句與中文重點 | 13 句 |

HANDOFF 07-26 自己記錄了這個轉向（[HANDOFF.md:512](HANDOFF.md:512)：「之後遇到類似的雜誌/新聞式課文，比照這次的做法」），**但沒有回頭處理第 17 課**。

兩篇都有標出處，情況不算糟；但正式站是**公開的 GitHub Pages**（不是私人筆記），第 17 課那 30 句等於把一篇雜誌文章的法文原文完整重刊。**建議照第 19 課的模式改寫**：保留全部文法示範句（passé composé 三種助動詞情境正是這課的重點，一句都不用少）＋中文內容摘要，刪掉純敘事、對學習沒有增益的整段原文。

**屬內容改寫，不自行動手**，等 Owen 決定。

---

## ✅ 階段二：已完成的低風險修正（各自獨立 commit）

| commit | 內容 | 驗證方式 |
|--------|------|----------|
| `8195270` | **A-5**：清空 `gram_rules.js` 的 `opinions` 佔位點 `topics`，解除對 `connectors-pour-parceque` 的誤鎖 | node 模擬 `getPool()` 過濾：該 topic 可出題數 **0→5**；`GRAM_POINTS` 仍 32 點、`futur-proche` 維持鎖住（0 題）未被波及；`node --check` 語法通過；確認 `map.html:792`／`codex.js` 對空 topics 的處理跟其他 12 個佔位點一致 |
| `fb63a48` | **B-3**：`dashboard.html` 錯題本 `SRC` 對照表補 `answercard:'🗣️ 答案卡'` | grep 確認 `answer_card.html:423` 寫入的 `src:'answercard'` 與對照表 key 一致 |

**全程沒有啟動 preview、沒有執行任何寫入 `clb7_*` 的操作**，`sync_supabase.js` 的 `ROOM` 從頭到尾是正式值 `owen-clb7-k9f3a72q`（commit 前已 grep 確認、repo 無 `TEST-DO-NOT-USE` 殘留）。對雲端只做過唯讀 GET。

---

## 🙋 需要 Owen 決定的清單（依建議優先序）

> 這些全部**沒有動手**。每一條都可以只回「做／不做／再說」。

**先決定這三個（影響最大）：**

1. **處方要不要瘦身？**（A-1）13 步 ≈ 110–145 分鐘，實際完成率中位數 20%。建議拆成「每日必做 5 步＋輪替 2 步」。
2. **口說要不要補？**（A-3）四科裡唯一沒有練習步驟、沒有評分、RUBRICS 的尺沒人實作的一科。兩條路：① `speaking.html` 照 `writing.html` 抄評分迴路 ② 排一個「盲說 2 分鐘」步驟進處方。
3. **雲端保護要不要做？**（C-1）建議先做「縮水保護」（改 `push()`，直接堵住三次事故的機制），再做 repo 每日快照。

**接著（各自獨立，做不做都不影響上面）：**

4. futur proche 現在算學過了嗎？（A-6）—— 影響一行設定，但現在 quiz 鎖住、複習卡照練，狀態是矛盾的。
5. `relatifs`（3 題）／`imparfait`（5 題）的打字題池要不要補到 8 題以上？（A-7）——07-24 的「連兩天同題目」bug 對這兩點仍是活的。
6. 造句練習要不要排除跟複習卡重複的 83 句？（A-4）
7. ⚡快速測驗（247 題）要接進 dashboard 統計，還是明確定位成支線？（B-8）
8. Owen 手寫筆記（`fr_*` 六個 key）要不要納入同步與備份？（C-3）
9. 4 個孤兒檔案照 B-5 的建議處置嗎？（特別是 `index.html` 現在是正式站首頁）
10. `table_drill` 的錯題要不要收進今日錯題本？（B-4，順帶決定 `logWrong` 要不要抽共用檔）
11. `reading.html` 要不要補 guided 自動開篇？（B-1）
12. `table_drill`／`reading`／`verb_sprint` 要不要補懸浮回饋？（B-2）
13. 第 17 課課文要不要改成摘要＋短引用？（C-6）
14. 🚩檢舉清單與 Answer Card 升級待辦這兩條「每次 session 要檢查」的儀式，實查兩份清單從未被建立過——要保留還是從 HANDOFF 拿掉？（C-4／C-5）
